<Project Sdk="Microsoft.NET.Sdk.Web">

  <ItemGroup>
    <ProjectReference Include="..\Pegasus.Core\Pegasus.Core.csproj" />
    <ProjectReference Include="..\Pegasus.Infrastructure\Pegasus.Infrastructure.csproj" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="Pegasus.IntegrationTests" />
    <InternalsVisibleTo Include="Pegasus.ArchitectureTests" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <RuntimeIdentifiers>linux-x64;win-x64</RuntimeIdentifiers>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <NoWarn>$(NoWarn);NU1510</NoWarn>
    <UserSecretsId>63edeba7-ecac-4928-8df3-071d2cf276bf</UserSecretsId>
    <!-- ADR-0028 / DELIV-012: the renderer runs in process inside this Web
         container, so the published image must carry the pinned Chromium
         build and its native Linux dependencies and fonts. The
         playwright/dotnet base image tag tracks $(PlaywrightVersion)
         (defined once in Directory.Build.props, shared with
         Pegasus.Infrastructure's Microsoft.Playwright package reference) so
         a future Playwright bump cannot silently desynchronise the base
         image from the pinned package. -->
    <ContainerBaseImage>mcr.microsoft.com/playwright/dotnet:v$(PlaywrightVersion)-noble</ContainerBaseImage>
  </PropertyGroup>

  <ItemGroup>
    <Content Remove="wwwroot\lib\**" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Azure.Extensions.AspNetCore.DataProtection.Blobs" />
    <PackageReference Include="Azure.Identity" />
    <PackageReference Include="Microsoft.ApplicationInsights.AspNetCore" />
    <PackageReference Include="ModelContextProtocol.AspNetCore" />
    <PackageReference Include="OpenIddict.AspNetCore" />
    <PackageReference Include="System.Security.Cryptography.Xml" />
    <PackageReference Include="Microsoft.EntityFrameworkCore.Design">
      <PrivateAssets>all</PrivateAssets>
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
    </PackageReference>
  </ItemGroup>

</Project>
