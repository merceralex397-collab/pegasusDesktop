name: repository-check

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  changes:
    # Path detection only: no application code runs here, so this job is not
    # Linux-development evidence (docs/runbook.md "Supported platform").
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      build: ${{ steps.detect.outputs.build }}
      infrastructure: ${{ steps.detect.outputs.infrastructure }}
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Require main history to be contained in dev
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        shell: pwsh
        env:
          BEFORE_SHA: ${{ github.event.before }}
          HEAD_SHA: ${{ github.sha }}
        run: |
          git fetch --no-tags origin refs/heads/dev:refs/remotes/origin/dev
          ./scripts/Test-MainBranchHistory.ps1 -Before $env:BEFORE_SHA -Head $env:HEAD_SHA -ReleaseBranch origin/dev
      - name: Detect whether a build-relevant path changed
        id: detect
        shell: pwsh
        run: |
          if ('${{ github.event_name }}' -eq 'pull_request') {
            $base = '${{ github.event.pull_request.base.sha }}'
            $head = '${{ github.event.pull_request.head.sha }}'
          } else {
            $base = '${{ github.event.before }}'
            $head = '${{ github.sha }}'
          }
          $forceAll = $true
          $files = @()
          if ($base -and $base -notmatch '^0+$') {
            $files = @(git diff --name-only "$base...$head")
            if ($LASTEXITCODE -eq 0) {
              $forceAll = $false
            }
          }
          $flags = ./scripts/Get-CiChangeFlags.ps1 -ChangedPath $files -ForceAll:$forceAll
          "build=$($flags.Build.ToString().ToLowerInvariant())" >> $env:GITHUB_OUTPUT
          "infrastructure=$($flags.Infrastructure.ToString().ToLowerInvariant())" >> $env:GITHUB_OUTPUT
      - name: Test SQL shard assignment
        shell: pwsh
        run: ./scripts/Test-TestShard.ps1
      - name: Migration runtime-grant check
        shell: pwsh
        run: ./scripts/Test-MigrationGrants.ps1
      # The release route runs Test-AzureDeploymentPlan, but CI never did, so a
      # merge could leave the route's own preflight failing and nobody found out
      # until a release was attempted. That is exactly what happened before
      # release 12: a grant-carrying migration merged without its database
      # bootstrap census entry and -Mode Local failed on dev. Local mode needs
      # no cloud credentials - only `az bicep build` - so it belongs here.
      - name: Azure deployment plan (Local)
        shell: pwsh
        run: ./scripts/Test-AzureDeploymentPlan.ps1 -Mode Local

  documentation:
    # The one lane every change set runs, including change sets that touch no
    # build-relevant path (docs/engineering.md "Branches and delivery"). It
    # stays on Windows because Test-Path is case-insensitive there and
    # case-sensitive on Linux, so moving it would quietly change the rule.
    runs-on: windows-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Markdown placement regression tests
        shell: pwsh
        run: ./scripts/Test-TestMarkdownPlacement.ps1
      - name: Documentation links
        shell: pwsh
        run: ./scripts/Test-DocumentationLinks.ps1

  local-development-scripts:
    # This parser contract is Windows-specific and must be called independently
    # of application, documentation, infrastructure, or cloud lanes.
    runs-on: windows-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v7
      - name: LocalDB lifecycle classifier tests
        shell: pwsh
        run: ./scripts/Test-PegasusPlatform.ps1

  reference-data:
    # The provider-reference generator and tests share one scripts-owned
    # component. This job is their executable gate; no application caller or
    # production import is implied.
    runs-on: windows-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v6
        with:
          python-version: '3.13'
      - name: Provider-reference generator tests
        shell: pwsh
        run: python -m unittest discover -s scripts/reference_data/tests -p 'test_*.py'

  infrastructure:
    # Local mode checks committed fail-closed invariants and compiles Bicep.
    # It does not authenticate, read Azure state, run azd or deploy anything.
    needs: changes
    if: needs.changes.outputs.infrastructure == 'true'
    runs-on: windows-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7
      - name: Change-classification regression tests
        shell: pwsh
        run: ./scripts/Test-CiChangeFlags.ps1
      - name: Validate infrastructure plan locally
        shell: pwsh
        run: ./scripts/Test-AzureDeploymentPlan.ps1 -Mode Local

  unit:
    # These projects run whole and unfiltered: the unit and architecture
    # projects do not declare a single test trait, while the contract project
    # is explicitly selected so CI cannot silently omit the new boundary test.
    needs: changes
    if: needs.changes.outputs.build == 'true'
    runs-on: windows-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions/dotnet-build
      - name: Test
        shell: pwsh
        # Chained, not two lines: pwsh reports only the last command's exit
        # code, so a failing first project would otherwise pass the step.
        run: >
          dotnet test ./tests/Pegasus.Core.Tests/Pegasus.Core.Tests.csproj --configuration Release --no-build &&
          dotnet test ./tests/Pegasus.ArchitectureTests/Pegasus.ArchitectureTests.csproj --configuration Release --no-build &&
          dotnet test ./tests/Pegasus.Api.ContractTests/Pegasus.Api.ContractTests.csproj --configuration Release --no-build --filter "Category=Contract"

  sql-integration:
    # Every integration test that is not a browser test. `Category=Browser` and
    # `Category!=Browser` are a complement pair, so this lane and `browser`
    # together select exactly what the single validate job used to select.
    # Two levels of overlap, because neither alone is enough on a four-vCPU
    # runner: test classes run in parallel within a shard, capped by
    # tests/Pegasus.IntegrationTests/xunit.runner.json, and the shards divide
    # whole classes across runners. Measured: the whole lane parallel on one
    # runner is 11m55s of tests, against ~4 minutes per shard.
    needs: changes
    if: needs.changes.outputs.build == 'true'
    runs-on: windows-latest
    timeout-minutes: 20
    strategy:
      # One shard's failure must not hide another shard's coverage report.
      fail-fast: false
      matrix:
        shard: [1, 2, 3]
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions/dotnet-build
      - name: Test
        shell: pwsh
        run: >
          ./scripts/Invoke-TestShard.ps1
          -Project ./tests/Pegasus.IntegrationTests/Pegasus.IntegrationTests.csproj
          -Filter "Category!=Corpus&Category!=Browser"
          -Shard ${{ matrix.shard }} -ShardCount 3
      - name: Retain what this shard enumerated and ran
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: test-shard-${{ matrix.shard }}
          path: artifacts/test-shards/
          if-no-files-found: error

  sql-integration-coverage:
    # Proves the shards reassemble into exactly the tests the lane's filter
    # selects. It matters more now than the count alone: `dotnet test` exits 0
    # when a filter matches nothing, so a shard that quietly ran none of its
    # assigned tests would otherwise pass.
    needs: [changes, sql-integration]
    # `always()` so a failing shard still gets its coverage reported, but never
    # when the lane was path-skipped.
    if: always() && needs.changes.outputs.build == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v7
      - uses: actions/download-artifact@v6
        with:
          pattern: test-shard-*
          merge-multiple: true
          path: artifacts/test-shards
      - name: Every enumerated test ran in exactly one shard
        shell: pwsh
        run: ./scripts/Invoke-TestShard.ps1 -VerifyPartition -ShardCount 3

  browser:
    needs: changes
    if: needs.changes.outputs.build == 'true'
    runs-on: windows-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions/dotnet-build
      - name: Cache the pinned Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~\AppData\Local\ms-playwright
          key: ms-playwright-${{ runner.os }}-${{ hashFiles('tests/Pegasus.IntegrationTests/packages.lock.json') }}
      - name: Install pinned Playwright Chromium
        # Never gated on a cache hit: the installer is idempotent and repairs a
        # partial cache, which is what keeps a poisoned cache from being trusted.
        shell: pwsh
        run: pwsh ./tests/Pegasus.IntegrationTests/bin/Release/net10.0/playwright.ps1 install chromium
      - name: Test
        shell: pwsh
        # Half the lane cap: each browser test starts a Chromium, a loopback
        # Kestrel host, and its own restored database, and four of those at
        # once on a four-vCPU runner is what makes navigation waits time out.
        # This lane is not on the critical path, so the headroom is free.
        run: >
          dotnet test ./tests/Pegasus.IntegrationTests/Pegasus.IntegrationTests.csproj
          --configuration Release --no-build --filter "Category=Browser&Category!=Corpus"
          -- xUnit.MaxParallelThreads=2
