2026-08-27 — Read current origin/dev: Pegasus.Api.ContractTests has the existing WebApplicationFactory and OpenAPI tests; DesktopGateway currently exposes no POST/PUT/PATCH/DELETE /api/v1 command endpoints, so the literal coverage table is initially empty by design. EndpointDataSource catalogue + symmetric guard will make the first future command endpoint fail until its explicit row is added. GWY-003 (the future auth-right metadata/filter) and later endpoint tickets are not merged, so this ticket will establish the reusable contract harness without inventing endpoint behavior or duplicate authentication/business policy.
