# Open questions — PLAT-002

- [ ] Confirm that desktop go-live has reached the point where `claudeuiverification` must be retired, and provide explicit exact-target approval for the production `Pegasus.Web` deployment that carries the `Removed` configuration. Current operator policy forbids cloud/deployment writes until the full refactor is complete, so this cannot be answered or executed in the current run.
- [ ] After the approved release, hand back the release number, image digest, revision suffix, and credential-free evidence that sign-in as `claudeuiverification` is refused. Do not provide or copy the disclosed password.

## Parked (explicitly deferred)

None.
