2026-08-28 implementation checkpoint: confirmed `EfCaseWorkflowStore` writes `CaseReportVersionLedgers`, and `20260827231948_IssuedReportVersionEvidenceLedger` grants Worker only `SELECT, UPDATE`. Added only `src/Pegasus.Infrastructure/Persistence/Migrations/20260828074800_GrantWorkerCaseReportVersionLedgerInsert.cs`; it guards on SQL Server, validates the managed Worker role, grants only `INSERT` in Up, and revokes only `INSERT` in Down. `dotnet restore Pegasus.slnx --locked-mode` passed. `dotnet build Pegasus.slnx --configuration Release --no-restore -p:UseSharedCompilation=false -p:BuildInParallel=false -p:NodeReuse=false` passed. `dotnet test tests/Pegasus.ArchitectureTests/Pegasus.ArchitectureTests.csproj --configuration Release --no-restore -p:UseSharedCompilation=false -p:BuildInParallel=false -p:NodeReuse=false --verbosity minimal` passed: 111 total, 111 succeeded, 0 failed, 0 skipped. `pwsh ./scripts/Test-MigrationGrants.ps1` passed: 72 migration files checked. Branch diff is one migration only; no production code, scripts, CI, cloud, deployment, upstream, or corpus changes.

2026-08-28 test-analysis correction: the independent test pass first found `Test-AzureDeploymentPlan.ps1 -Mode Local` failed because the new grant-carrying migration was not named in the bootstrap matrix, and the focused Worker permission expectation omitted `CaseReportVersionLedgers:INSERT`. Updated only the existing bootstrap expected-state block and existing integration-test expectation. Re-run passed: local deployment-plan validation reports Worker Disabled settings render `true`; focused `LatestMigrationGrantsIssuedReportVersionLedgerToItsRuntimeCallers` passed 1/1. Aristotle's independent simplification pass found no code changes needed.

2026-08-28 review/validation checkpoint: Boyle independently reviewed exact PR #38 head 59af1b21. No implementation defect was found; merge-readiness was held only for incomplete exact-head CI and combined evidence. A clean detached tree combining exact PLAT-018 fca04a91, PLAT-030 c599a42, and PLAT-031 59af1b21 passed locked restore, focused RuntimeGrantCompositionTests 10/10, full ArchitectureTests 121/121, Test-MigrationGrants 73/73, local deployment-plan validation, and git diff --check. CI run 33153323761 still has SQL shards 2 and 3 running; no merge yet.
