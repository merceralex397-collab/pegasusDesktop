# Plan — ENG-002: upstream:ENG-015 · Export the field values EVA expects

## Governing documents

- `docs/frd/frd-07-eva-and-external-engineering-handoff.md`

## Chosen approach

Correct four of the thirteen exported EVA values in the mapping layer: `Reference` carries the work provider's claim number instead of our case reference, `Inspection Address` is a six-line block instead of one line, `Vehicle Model` carries the make with the model on every branch, and the `Mileage Unit` casing (`Miles`/`Km`) is settled and recorded. The key set and key order do not change.

## Routing and constraints

- Future owner follows the ticket’s stated project boundary and repository task workflow. Reuse existing Core policy/ports before adding any abstraction.


## Ordered implementation steps

1. Orient. Read this body in full — the upstream ticket is reproduced above verbatim and is the requirement. Confirm the imported `upstream:ENG-014` has merged; if it has not, stop and say so, because changing the field values first makes the archive bytes and `InputFingerprint` move twice. Re-read the four mapping sites listed under Fork repository evidence at current `dev` (the fork is behind upstream; the sync may have moved them). Call `get_doc_gates <this ticket id>`, then `take_ticket` with branch `task/<this ticket id>-eva-field-values` and worktree `../pegasus-worktrees/<this ticket id>` from `origin/dev`.
2. Record the research the `feature` profile owes: for each of the thirteen keys, the accepted case value it maps from, whether it can be missing, and what the hand-off does when it is. Name the two retained samples as the comparison corpus and state plainly that the upstream body's third sample is not in the fork tree.
3. **`Reference`.** Replace the hard-wired tuple at `src/Pegasus.Infrastructure/Persistence/EvaHandoffStore.cs:686-689` with `FromCaseField(caseData.Claim.Number, static value => value)`, matching the twelve fields around it. Done means the exported `Reference` reads the work provider's claim number (`AMA/47857/1`-shaped, per `tests/Pegasus.IntegrationTests/QdosMappingExtractionTests.cs:32-52`) and never `ap.QDOS26015`.
4. **Decide and record the fail-closed consequence of step 3 before merging.** Today `Reference` is the case identity and is always present; sourced from `caseData.Claim.Number` it becomes `MissingEvidence` when no accepted claim number exists, and `CaseEvaMapping`'s validation (`CaseEvaMapping.cs:216-231`) then yields "Reference does not have accepted evidence" and the hand-off refuses to generate. That is a real behaviour change for a case whose claim number was never captured. **Operator step**: put the choice to the operator — refuse generation (fail closed, consistent with the other twelve fields) or fall back to the case reference with the divergence recorded — and record the answer in the plan document with its date. Do not guess; do not implement a silent fallback.
5. **`Inspection Address` — six lines.** Add one `Format` helper to `src/Pegasus.Core/Eva/CaseEvaMapping.cs` producing exactly six slots (addr1-addr5 plus postcode) joined by `\n`, applied where the value is produced so the JSON and the in-memory `EvaFieldProvenance[]` agree, with a named, commented exemption from `NormalizeValue`'s `.Trim()` (`:251-263`) for this one field. One helper in one place — the one-list-per-concept rail.
6. **Re-expressed for the fork: do not change the shared constant.** The upstream approach says "change only the EVA-facing literal", but in the fork `CaseEvaMapping.ImageBasedAssessment` (`:94`) is **both** the EVA-facing text and the `Ordinal` comparison target for the case's own stored value — at `CaseEvaMapping.cs:31` (`EvaAddressResolution.IsResolved`) and `EvaHandoffStore.cs:740` (`ResolveInspection`'s `PhysicalAddress` guard). Editing it in place would make every image-based case fail to resolve. So introduce a **separate output literal** (for example `EvaInspectionAddressText = "Image-based Assessment"`, hyphen and lowercase `b`, exactly as `reference/eva_information/AX_SP58WVO.json:10` carries it) used only when writing the EVA value, leave `ImageBasedAssessment` and `src/Pegasus.Core/Address/Ext18InspectionAddressPolicy.cs:12` untouched, and state this divergence from the upstream approach in the plan document with the two comparison sites as the reason. Done means an image-based case still resolves and exports `"Image-based Assessment\n\n\n\n\n"`, and a physical-address case exports its six slots.
7. **`Vehicle Model`.** In the fork tree at the read date `VehicleModel` (`EvaHandoffStore.cs:753-770`) already prepends the make via `Combine` on the confirmed-vehicle branch and has no case-field fallback, so there may be nothing to change: check the tree after the sync, and if a fallback branch reading the model alone now exists, prepend the make there too through the same `Combine` (`:835`) rather than a second join. Either way add the regression test that pins make-then-model in the exported value, and record in the plan document which case applied.
8. **`Mileage Unit`.** `EvaHandoffStore.cs:698-706` emits `"miles"` / `"kilometres"`; the samples carry `"Miles"` (`AX_SP58WVO.json:14`) and `"Km"` (`Final Format Example 02.json:14`). **Operator step**: settle whether EVA is strict about the casing — by an actual EVA import of a generated package, or by the operator's answer — and record it with its date before implementing. If it is to match, map `VehicleMileageUnit.Miles => "Miles"` and `VehicleMileageUnit.Kilometres => "Km"` in that one switch and nowhere else. Do not change the casing on the strength of the samples alone; they may be artefacts of the predecessor extractor.
9. Tests. Add a mapping-level test in `tests/Pegasus.Core.Tests/Qdos/` pinning all four corrected values, and extend `tests/Pegasus.IntegrationTests/EvaHandoffPersistenceTests.cs` so a generated bundle's exported JSON carries the claim number in `Reference`, six lines in `Inspection Address` and make-plus-model in `Vehicle Model`. Keep the existing fail-closed assertions green: the hand-off must still refuse to generate on unaccepted evidence. Run the QDOS corpus suites (`QdosMappingExtractionTests`, `QdosExtractionCoverageTests`) to prove no extraction expectation moved.
10. Documentation. Add one sentence to `docs/frd/frd-07-eva-and-external-engineering-handoff.md` beside the thirteen-key list (`:19-33`) stating that `Reference` carries the work provider's claim number — EVA's `Claim no` — and not our `Case/Po` case reference, and that `Inspection Address` is a six-slot block. Load `kanmer-docs` first. Do not restate the key list and do not touch the manifest sentences (the imported `upstream:ENG-014` owns those).
11. Record the four upstream open questions on this ticket rather than answering them in code (see Guardrails). Two are settled here as operator steps (mileage casing, and the `Reference` fail-closed consequence); the other two — `Accident Circumstances` from the damage-area block, and the bare `Date:` label — are explicitly **out of scope** and are raised as their own tickets if the operator wants them.
12. Run the simplification pass over the branch diff (`AGENTS.md` step 4), record it under a dated `## Simplification pass` heading in the plan document, and open the PR into `dev`. No new abstraction, flag or configuration knob: four values, one formatter, one switch.

## Acceptance conditions

- [ ] The exported `Reference` carries the work provider's claim number from `caseData.Claim.Number`; `ap.QDOS26015`-shaped case references no longer appear in the key.
- [ ] The behaviour when no accepted claim number exists is the operator's recorded answer from step 4, implemented exactly and stated in the plan document — not a silent fallback.
- [ ] `Inspection Address` exports exactly six slots; an image-based case exports `"Image-based Assessment\n\n\n\n\n"` byte-for-byte as `reference/eva_information/AX_SP58WVO.json:10` carries it, and a physical-address case exports addr1-addr5 plus postcode.
- [ ] `CaseEvaMapping.ImageBasedAssessment` and `Ext18InspectionAddressPolicy.ImageBasedAssessment` are unchanged, and every image-based case still resolves (`EvaAddressResolution.IsResolved` true).
- [ ] `Vehicle Model` carries make and model on every branch that can produce it, through the single `Combine` join.
- [ ] `Mileage Unit` matches the operator's recorded decision from step 8, with the decision and its date in the plan document.
- [ ] The hand-off still fails closed on unaccepted evidence, and the QDOS corpus expectations are unchanged.
- [ ] `docs/frd/frd-07-eva-and-external-engineering-handoff.md` states whose reference `Reference` is.

## Verification

- [ ] `dotnet test --filter "FullyQualifiedName~Eva"` plus the QDOS suites
- [ ] Exported `Reference` reads the claim number, not the case reference
- [ ] `Inspection Address` splits to exactly 6 lines, first line
      `Image-based Assessment`
- [ ] `Vehicle Model` carries make and model
- [ ] The hand-off path still fails closed on unaccepted evidence
```

- **Fork repository evidence** (every line read 2026-08-24 at fork `main` `bbd1c549`; the upstream body's line numbers are upstream's and do **not** match the fork, which is behind by the commits [[DSK-01-10]] lands — use these):
  - `src/Pegasus.Infrastructure/Persistence/EvaHandoffStore.cs:668` `MapAcceptedCase`, called from `:53` and `:446`; the hard-wired `Reference` tuple is `:686-689` (`new(caseData.Identity.Reference, EvaEvidenceStatus.Accepted, $"case-identity:{caseId:D}", "case-reference/v1")`), the twelve siblings around it use `FromCaseField`/`FromVehicleField` (`:690-706`)
  - `:753-770` `VehicleModel(ConfirmedVehicleEvidence?)` — in the fork this branch **already** prepends the make through `Combine` (`:835`) and returns `MissingEvidence` when there is no confirmed vehicle record; the case-field fallback the upstream body describes at its `:881-885` is not in the fork tree at the read date
  - `:698-706` the mileage-unit switch emitting `"miles"` / `"kilometres"`
  - `:709-751` `ResolveInspection`, whose `PhysicalAddress` guard compares the case's stored address `Ordinal` against `CaseEvaMapping.ImageBasedAssessment` (`:740`)
  - `src/Pegasus.Core/Eva/CaseEvaMapping.cs:94` `public const string ImageBasedAssessment = "Image Based Assessment";`, compared `Ordinal` at `:31` inside `EvaAddressResolution.IsResolved`; `:251-263` `NormalizeValue`, whose `.Trim()` strips trailing newlines; `:116` `MapForProduction`; `:233-249` `RequiredMappedFields`; `:216-231` the accepted-evidence validation that yields "<field> does not have accepted evidence"
  - `src/Pegasus.Core/Address/Ext18InspectionAddressPolicy.cs:12` — the second definition of the same literal, the one written into the case
  - `src/Pegasus.Core/Cases/CaseDataContracts.cs:72` `CaseClaimData(CaseField<string> Number)` and `:118` the `Claim` member of `CaseDataProjection` — so `caseData.Claim.Number` is directly available to the mapping
  - `src/Pegasus.Core/Intake/DirectProviders/Qdos/QdosInstructionExtractionPolicy.cs:26-28` — the claim-number labels `Claim Number`, `Claim No`, `Claim Reference`, `Claim Ref`, `Our Reference`, `Our Ref`; `:49-51` the instruction-date labels (`Instruction Date`, `Date of Instruction` — a bare `Date:` is absent); `:317` `CircumstancesParagraph` with its terminator regex at `:340`
  - `tests/Pegasus.IntegrationTests/QdosMappingExtractionTests.cs:32-52` — the corpus expectations already pin provider claim-reference shapes (`AMA/47857/1`, `AKH/SBU/47856/1`, `JF//47847/1`)
  - `reference/eva_information/AX_SP58WVO.json:4` `"Vehicle Model": "HONDA CIVIC TYPE-R GT I-VTEC"`, `:6` `"Reference": "1070277"`, `:10` `"Inspection Address": "Image-based Assessment\n\n\n\n\n"`, `:12` `"VAT Status": "No"`, `:14` `"Mileage Unit": "Miles"`; `reference/eva_information/Final Format Example 02.json:10` `"109 Valley View\nHoole\n\n\n\nCH490DJ"` and `:14` `"Km"`
  - `reference/eva_information/eva_information.md:35` and `:39` — the `Case/Po` versus `Claim no` distinction. The third sample the upstream body names (`docs/json-extraction-parity/ap.QDOS26015/old-extraction-working/QDOS_NX14AXY.json`) is **not in the fork tree** at the read date; two known-good samples are.
  - `docs/frd/frd-07-eva-and-external-engineering-handoff.md:19-33` — the thirteen keys in serialization order, with `Reference` as key 5 and no statement of whose reference it is
- **Board context**: [[DSK-05-15]] (FEAT-015) asserts the thirteen field values against the known-good samples and cannot ship correctly until this lands; [[DSK-01-05]] (FND-018) owns parity row `PAR-18`, which must widen to bundle content — this ticket does not edit that row; [[DSK-01-09]] (FND-022) assigns this ticket's phase and adds it to the carry-over register; [[DSK-05-17]] and [[DSK-07-16]] consume case data but not this mapping.
- **Binding decisions**: **D-001** — nothing arrives from upstream for this ticket, so the fork board is its only home. **L-01** — the fix is server-side inside `Pegasus.Web`'s solution and serves the Razor pages and `/api/v1` at once; no desktop code changes. **L-02** — verification is the local production-mimicking stack plus the retained QDOS corpus, never an Azure test resource.
- **Depends on**: the imported `upstream:ENG-014` — it must merge first so the archive bytes and `InputFingerprint` change once, and so this ticket's layout assertions run against the indented writer. `DSK-01-10` — re-read the four mapping sites after the sync before planning, since the fork is behind upstream here.

## Risks and boundaries

- **Azure**: no write, and no Azure read — this ticket has no cloud surface. Verification is the local stack and the retained corpus under **L-02**.
- **Scope boundary**: may touch `src/Pegasus.Infrastructure/Persistence/EvaHandoffStore.cs` (the mapping only), `src/Pegasus.Core/Eva/CaseEvaMapping.cs`, the EVA and QDOS test files, and `docs/frd/frd-07-eva-and-external-engineering-handoff.md`. Must **not** change `CaseEvaMapping.ImageBasedAssessment` or `src/Pegasus.Core/Address/Ext18InspectionAddressPolicy.cs:12` (both are comparison targets for the case's stored value), the thirteen keys or their order, `EvaBundleSchema`'s packaging (the imported `upstream:ENG-014` owns it), any Razor page model, or any desktop project.
- **Blocking**: this **unblocks** [[DSK-05-15]] (FEAT-015), whose thirteen-field acceptance criterion cannot pass while four values are wrong, and through it [[DSK-05-22]], [[DSK-05-25]], [[DSK-07-18]], [[DSK-08-08]] and [[DSK-08-16]]. It is **blocked by** the imported `upstream:ENG-014` (sequence ENG-014 then ENG-015 so the archive bytes change once) and follows [[DSK-01-10]]'s sync. [[DSK-01-09]] (FND-022) assigns its phase; do not invent one here.
- **Open questions carried from upstream** (recorded here, not answered in code): (a) **Mileage Unit casing and CRLF** — the samples show `Miles`/`Km` and CRLF but may be predecessor artefacts; step 8 settles it by an actual EVA import or the operator's answer. (b) **`Reference` fail-closed behaviour** — step 4's operator decision; this one is new to the fork and must not be skipped. (c) **Accident Circumstances** — whether damage-area text should feed the key when no circumstances prose exists (`QdosInstructionExtractionPolicy.cs:317`/`:340`) is a business rule and is **out of scope**; raise its own ticket. (d) **Instruction Date** — the bare `Date:` label absent from `QdosInstructionExtractionPolicy.cs:49-51` makes every QDOS case default to the receipt date; **out of scope**, likely an intake ticket. (e) **VAT Status** — confirm with the operator that it is meant to be staff-entered rather than derived; recording the answer is enough, no code follows.
- **Traps**: the fork is behind upstream, so every line number in the upstream body is upstream's — re-derive them. The `Reference` change makes a previously-always-present value optional; that is the one place this ticket can break generation for a real case. `NormalizeValue`'s `.Trim()` will silently eat the address padding unless the exemption is explicit. The samples are evidence of shape, not a specification — the casing question is settled by an import, not by copying.
- **Simplification pass** (`AGENTS.md` step 4): required over this branch diff before the PR, recorded under a dated `## Simplification pass` heading in the plan document.
