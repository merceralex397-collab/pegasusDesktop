---
id: REL-003
type: ticket
title: >-
  DSK-09-03 · .appinstaller templates (pilot/prod) and validator
  eng/packaging/Test-AppInstaller.ps1
status: backlog
area: release-desktop
assignee: ''
profile: feature
labels:
  - desktop-conversion
  - plan-09
  - phase-2
  - tier-1
groups:
  - EPIC-010
  - HZN-003
links: []
docs_todo: true
archived: false
created: '2026-08-24T07:45:38.394Z'
updated: '2026-08-24T07:45:38.394Z'
---

## What

Add the two `.appinstaller` templates (`pilot`, `prod`) in the **2021 schema** under `eng/packaging/`, and the validator `eng/packaging/Test-AppInstaller.ps1` that fails on each of the eight named violations in the plan's validator outline, with fixture-based regression tests.

## Why

Proposal § 9.1 makes the signed package's launch update one of the two enforcement layers, and the `.appinstaller` file is the update contract every installed workstation re-reads on every launch. Only the 2021 namespace supports `ShowPrompt`, `UpdateBlocksActivation` and `HoursBetweenUpdateChecks`; Visual Studio emits the 2017/2 schema by default and **silently ignores** those settings, which would leave an obsolete client able to keep working. The validator is the only thing that catches that, plus a publisher mismatch, a non-monotonic version or a hash that does not match the manifest, before a release is published. [[DSK-09-04]] and [[DSK-09-05]] both call it.

## Source of truth

- Plan row: `docs/desktop/09-release-update-and-distribution/README.md` § 5 — `DSK-09-03`
- Plan detail: `docs/desktop/09-release-update-and-distribution/appinstaller-template.md` — § Template (2021 schema), § Per-channel variants, § Validator outline, § Hosting requirements, § Known behaviours
- Proposal: `docs/desktop/Pegasus_Native_Desktop_Design_Proposal.md` § 9.1 Two-layer enforcement, § 9.3 Operational controls
- Repository evidence: `scripts/Test-PegasusPlatform.ps1` and `scripts/Test-TestShard.ps1` (the repository's script-test conventions: `Set-StrictMode -Version Latest`, `$ErrorActionPreference = 'Stop'`, non-zero exit on failure); `docs/desktop/09-release-update-and-distribution/README.md` § 7 (traps this validator enforces)
- Binding decisions:
  - **D-003** — the feed is a UNC share, so the `Uri` values are `\\<host>\<share>\<channel>\…` paths, not HTTPS URLs.
  - **D-002** — `MainPackage/@Publisher` must equal the subject of the self-managed signing certificate.
- Depends on: `DSK-09-02` — the validator compares the `.appinstaller` against `desktop-release-manifest.json`, which that ticket defines.

## Routing

- **Subagent**: `pegasus-release-packager` — `.codex/agents/pegasus-release-packager.toml`
- **Skills**, loaded in this order: `pegasus-desktop` (`.agents/skills/project/pegasus-desktop/SKILL.md`) → `winui-packaging` (`.codex/skills/winui-packaging/SKILL.md`, vendored from `microsoft/win-dev-skills` v0.5.0 `f1028dd5`)
- **MCP**: Kanmer (`get_status`, `get_doc_gates`, `take_ticket`, `set_ticket_doc`, `append_scratch`, `move_item`); Microsoft Learn (`microsoft_docs_search`, `microsoft_docs_fetch`) for the App Installer update-settings and schema pages
- **Kanmer pipeline** for profile `feature`: `kanmer-research` → `kanmer-plan` → `kanmer-execute` → `kanmer-review` → `kanmer-verify` → `kanmer-closeout` (call `get_doc_gates <id>` before every move; a move crosses at most one gated boundary)
- **Reviewer**: `pegasus-desktop-reviewer` — an agent that did not implement (`AGENTS.md` § Repository task workflow step 5)

## Implementation steps

1. Orient: read `docs/desktop/09-release-update-and-distribution/appinstaller-template.md` **in full**, then the § 5 row and § 7 traps of the area plan. Call Kanmer `get_doc_gates` on this ticket id, then `take_ticket`.
2. Load `winui-packaging`. Use `microsoft_docs_search` for `App Installer update settings OnLaunch HoursBetweenUpdateChecks UpdateBlocksActivation` and `microsoft_docs_fetch` on <https://learn.microsoft.com/windows/msix/app-installer/update-settings> to confirm the attribute names and the `UpdateBlocksActivation` requires-`ShowPrompt` rule before writing the template.
3. Create `eng/packaging/Pegasus.appinstaller.template.xml` containing **exactly** this document — copy it verbatim, do not paraphrase or reformat:

```xml
<?xml version="1.0" encoding="utf-8"?>
<AppInstaller
    xmlns="http://schemas.microsoft.com/appx/appinstaller/2021"
    Uri="<feed>/<channel>/Pegasus.appinstaller"
    Version="<appinstaller-version>">

  <MainPackage
      Name="CollisionEngineers.Pegasus"
      Publisher="<publisher-subject-from-signing-certificate>"
      Version="<ver>"
      ProcessorArchitecture="x64"
      Uri="<feed>/<channel>/Pegasus_<ver>_x64.msix" />

  <!-- No <Dependencies>: the package is self-contained for .NET and the
       Windows App SDK (proposal §7.1). If a framework-dependent package is
       ever chosen, add the Microsoft.WindowsAppRuntime.<major> dependency
       here and host its MSIX alongside. -->

  <UpdateSettings>
    <!-- Check on every launch, show the prompt, and block activation until
         the update is taken. UpdateBlocksActivation requires ShowPrompt. -->
    <OnLaunch HoursBetweenUpdateChecks="0"
              ShowPrompt="true"
              UpdateBlocksActivation="true" />
    <!-- Also check roughly every 8 hours in the background (no UI). -->
    <AutomaticBackgroundTask />
    <!-- Allow the feed to move a workstation to a LOWER version (rollback). -->
    <ForceUpdateFromAnyVersion>true</ForceUpdateFromAnyVersion>
  </UpdateSettings>

  <!-- Optional fallbacks (max 10) used only if the Uri above is unreachable.
       Use a second host or path only if D-003 provides one. -->
  <!--
  <UpdateUris>
    <UpdateUri><feed-fallback>/<channel>/Pegasus.appinstaller</UpdateUri>
  </UpdateUris>
  -->
</AppInstaller>
```

4. Add `eng/packaging/New-AppInstaller.ps1`, which substitutes `<feed>`, `<channel>`, `<appinstaller-version>`, `<ver>` and `<publisher-subject-from-signing-certificate>` from `desktop-release-manifest.json` and writes `Pegasus.appinstaller` for the requested channel. Under D-003 the `<feed>/<channel>` substitution produces `\\<host>\<share>\<channel>` UNC paths — that is the decided form; there is no HTTP variant to support.
5. Create `eng/packaging/Test-AppInstaller.ps1` taking `-AppInstallerPath`, `-Channel`, `-ManifestPath`, `-PreviousAppInstallerPath` and a `-Rollback` switch. Implement these eight checks, each with its own named failure message, copied from `appinstaller-template.md` § Validator outline: (1) namespace is `http://schemas.microsoft.com/appx/appinstaller/2021`; (2) `Uri` equals `<feed>/<channel>/Pegasus.appinstaller` for the channel; (3) `Version` is strictly greater than the last published version for the channel (four-part numeric compare); (4) `MainPackage/@Name` is `CollisionEngineers.Pegasus`, `@ProcessorArchitecture` is `x64`, `@Version` equals the manifest version, `@Publisher` equals the signer subject recorded in the manifest; (5) `MainPackage/@Uri` resolves to a file whose SHA-256 equals the manifest hash; (6) `UpdateSettings/OnLaunch` has `HoursBetweenUpdateChecks="0"`, `ShowPrompt="true"`, `UpdateBlocksActivation="true"`, `ForceUpdateFromAnyVersion` is `true`, `AutomaticBackgroundTask` present; (7) no `Dependencies` element unless the manifest says framework-dependent; (8) rollback mode — a `MainPackage/@Version` lower than the previous is allowed only when `-Rollback` is passed **and** `ForceUpdateFromAnyVersion` is `true`.
6. Make the version compare a real four-part numeric compare (`[version]` cast on both sides), not a string compare — `1.0.9.0` must sort below `1.0.10.0`.
7. Output a pass/fail list to stdout and exit non-zero on any failure, so CI and `Build-DesktopRelease.ps1` can gate on the exit code.
8. Create `eng/packaging/fixtures/appinstaller/` with one fixture file per failure: `schema-2017.xml`, `wrong-uri.xml`, `version-not-monotonic.xml`, `publisher-mismatch.xml`, `hash-mismatch.xml`, `missing-showprompt.xml`, `unexpected-dependencies.xml`, `downgrade-without-rollback.xml`, plus `valid-pilot.xml` and `valid-prod.xml`.
9. Create `eng/packaging/Test-TestAppInstaller.ps1` (regression test of the validator) asserting that each fixture produces exactly its named failure and that the two valid fixtures pass, then exit non-zero if any expectation is unmet.
10. Run `pwsh ./eng/packaging/Test-TestAppInstaller.ps1` — expected exit code `0` with ten reported expectations met.
11. Record the dated `## Simplification pass` in the ticket plan document over this branch's own diff.

## Acceptance criteria

- [ ] Both channel templates use the 2021 namespace and carry `OnLaunch HoursBetweenUpdateChecks="0" ShowPrompt="true" UpdateBlocksActivation="true"`, `AutomaticBackgroundTask` and `ForceUpdateFromAnyVersion`.
- [ ] The generated `Uri` equals the exact UNC path the file is served from for that channel.
- [ ] The validator rejects each of the eight violations with a distinct named failure and exits non-zero.
- [ ] The `.appinstaller` `Version` compare is four-part numeric and strictly increasing.
- [ ] `MainPackage/@Uri`'s SHA-256 is compared against `desktop-release-manifest.json`.
- [ ] `-Rollback` is the only way a lower `MainPackage/@Version` passes.

## Verification

- [ ] `pwsh ./eng/packaging/Test-TestAppInstaller.ps1` — expected: exit code `0`; every fixture reports its expected named failure and the valid fixtures pass.
- [ ] `pwsh ./eng/packaging/Test-AppInstaller.ps1 -AppInstallerPath ./eng/packaging/fixtures/appinstaller/schema-2017.xml -Channel pilot -ManifestPath <fixture manifest>` — expected: exit code non-zero and the message naming the 2021-namespace check.
- [ ] `Select-Xml -Path ./eng/packaging/Pegasus.appinstaller.template.xml -XPath '/*'` — expected: the root element namespace is `http://schemas.microsoft.com/appx/appinstaller/2021`.

## Evidence tier

Tier 1 — Static/build/architecture. It obliges fixture-driven proof that the validator's failures fire; App Installer's real behaviour is proven later by the packaging suite [[DSK-08-10]] against the local feed.

## Documentation changes

- None in `docs/`. The template and validator outline already live in `docs/desktop/09-release-update-and-distribution/appinstaller-template.md`; if the implementation deviates from it, update that file in the same task.

## Guardrails

- **Azure**: no write.
- **Scope boundary**: `eng/packaging/**` only. Must not touch `src/`, `infra/`, or the gateway release scripts.
- **Traps**: the 2017/2 schema **silently ignores** `ShowPrompt`, `UpdateBlocksActivation` and `HoursBetweenUpdateChecks` — reject any namespace other than 2021. Never publish an `ms-appinstaller:?source=` protocol link; the protocol has been disabled by default since December 2023 — publish the `.appinstaller` file path. `UpdateBlocksActivation` requires `ShowPrompt="true"`. A `Publisher` that does not exactly match the signing certificate subject breaks installation. The `Uri` is baked into every installation: changing host, share or channel folder later forces a reinstall on every workstation.
- **Not this ticket**: MIME types, `Content-Length` and byte ranges are HTTP-only concerns and do **not** apply over SMB (D-003). Do not add header checks to this validator.
- **Simplification pass** (`AGENTS.md` step 4): required over this branch diff before the PR, recorded under a dated `## Simplification pass` heading in the plan document.

## Outcome

_Filled at closeout._
