2026-08-25 checkpoint: Microsoft Learn queue-trigger evidence fetched from the official queue-trigger polling page. It documents exponential backoff, local default max polling interval 2s, visibilityTimeout retry behavior, and poison handling. Tracked host.json uses batchSize 4, newBatchThreshold 2, visibilityTimeout 5m, maxDequeueCount 5, maxPollingInterval 2s; intake-work is the durable intake queue.
