Verify-after-sync decision — 2026-08-25: live read-only `git ls-remote https://github.com/collisionengineers/pegasus.git refs/heads/dev refs/heads/main refs/heads/task/case-021-observed-images` returned dev/main at `7afd18037acfa78927c4b4ffdf8e0f74c7ecc688` and no task branch ref. Fork worktree HEAD is `5770eb21`; `IntakeAllocation.cs` still contains `AutomaticCompleteness` with `ImagesComplete: true` at lines 224-228 and the call site passes the constant at line 269. Result: NOT ARRIVED; this ticket must carry the full fix. No upstream write or Azure operation performed. Operator confirmation was not supplied; the live remote check is recorded as the evidence used for the scope decision.

Implementation/validation checkpoint — the Core path now computes `ImagesComplete` from `InstructionEvidenceImages.Select(receipt.AssetRecords).Count > 0`; no second image rule or new abstraction. Focused Core test run passed 12/12; full `Pegasus.Core.Tests` passed 921/921; focused LocalDB integration test `AutomaticAllocationWithoutPhotographsPersistsNotReadyWithScheduledChase` passed 1/1. Existing coverage confirmed for the three known consequences: `InstructionEvidenceImagesTests.SelectsAttachedImagesAndLargeEmbeddedImagesOnly` (inline/source/embedded/attached selection), `TheThresholdIsABoundaryNotAGuess` (under-floor embedded image), `QdosTwentySixZeroZeroEightsLetterheadBannersAreNotEvidence` (banner shape), and `GroupedImageIntakeConcurrencyTests` group-settlement/recovery facts for later image receipts. No Azure/cloud writes.

Repository validation checkpoint — `dotnet restore ./Pegasus.slnx --force-evaluate` passed; `dotnet build ./Pegasus.slnx --configuration Release --no-restore` passed with 0 warnings/0 errors; ArchitectureTests passed 99/99; full non-browser/non-corpus IntegrationTests passed 872/872 with 3 expected skips (875 total). Initial full integration run exposed one fixture hash mismatch because a seeded pending command asserted images true; corrected that fixture to observed false, focused recovery test passed 1/1, and the full rerun passed. Commit `29c1b83b` contains the three-file scoped implementation/test diff.

PR attempt from `case-001-observed-images` targeting `dev` failed with exact GitHub response: `pull request create failed: GraphQL: must be a collaborator (createPullRequest)`. Branch is pushed at `29c1b83b030f402c349576e6fc4f7e1ab1184430`; post-implementation-report is written; no PR/CI merge or proof claim is made. Ticket remains blocked at the PR/review handoff pending repository collaborator permission or an authorized PR workflow.

2026-08-25 independent review: NEEDS CHANGES. Blocker: the cited grouped-image test does not allocate an instruction case, process a later image receipt, and assert the allocation-time completeness remains unchanged. Warning: the new IntakeAllocation comment inaccurately says the old code recorded all four fields false; origin/dev had InstructionComplete/ImagesComplete true and only staff flags false. Returning to implementation evidence; add a real LocalDB later-image association test and correct the comment, then rerun focused/full relevant validation and obtain a fresh independent review.

2026-08-25 review PASS recorded. Files map reconciled to AllocateDefinitiveIntakeTests.cs and QdosAllocationRecoveryTests.cs; original upstream map/simplification move explained. Commit 995bf671 pushed. Fresh gates passed and ticket moved implementing→review at 2026-08-25T06:43:30.068Z; board commits now include 29c1b83b and 995bf671. PR retry exact result: gh pr create --base dev --head case-001-observed-images -> pull request create failed: GraphQL: must be a collaborator (createPullRequest). No PR, CI, merge, proof, or done claim.

2026-08-25 — Fresh branch validation before PR: `dotnet restore ./Pegasus.slnx` passed; `dotnet build ./Pegasus.slnx --configuration Release` passed (0 warnings/errors); Core tests passed 921/921; Architecture tests passed 99/99. Full non-corpus/non-browser LocalDB integration is still running. Independent `pegasus-test-engineer` audit found two blocking coverage gaps: assert the unchanged instruction/staff-confirmation fields through the real allocation path, and assert that due work remains scheduled after a later image receipt. It also requires CI-compatible SQL shard/TRX evidence. The next scoped change is limited to the two mapped test files; no production-policy change is planned.
