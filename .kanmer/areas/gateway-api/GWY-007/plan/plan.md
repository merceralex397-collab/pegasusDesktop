# Plan — GWY-007: DSK-03-07 · Case read endpoints: paged list/search, sectioned detail, case history and audit

## Governing documents

- No canonical document is linked yet. Retain the ticket's existing `docs_todo` state; do not invent or link a proposed desktop ADR.

## Chosen approach

Add the read half of the case surface to `/api/v1`: a paged, sorted, filtered case list, a sectioned case detail whose sections load independently, and the case history/audit reads — all projecting the same Core query ports the Razor pages use, and sourcing the four case-identity facts from the case itself rather than the intake draft (upstream CASE-020).

## Routing and constraints

- Future owner: `pegasus-gateway-dev`; tests: `pegasus-test-engineer`; independent review: `pegasus-desktop-reviewer`.
- Use `dotnet-webapi`, `optimizing-ef-core-queries` where the ticket changes a query, and `run-tests` for the actual runner profile. The project decision overrides generic “service per endpoint” advice: route handlers translate to existing `Pegasus.Core` ports; no second policy/service layer is introduced.
- The shared EPIC context binds this to versioned `/api/v1` route groups in the existing `Pegasus.Web`, the existing rate-limiter mechanism, an OpenAPI snapshot, and no Azure write.

- Microsoft Learn (fetched 2026-08-24): [ASP.NET Core OpenAPI support](https://learn.microsoft.com/aspnet/core/fundamentals/openapi/overview?view=aspnetcore-10.0) confirms first-party OpenAPI generation. Use the repository’s planned committed snapshot and contract-test flow rather than adding a parallel API documentation path.


## Ordered implementation steps

1. Orient. Read the four endpoint rows quoted above in `docs/desktop/03-gateway-api-and-data/endpoint-map.md` § Cases and § Administration and audit, plus `README.md` § 3 rows *Paging/filter/sort* and *Case detail*. Read the upstream CASE-020 body named under Source of truth, and the upstream-to-board join beneath it. Then `get_doc_gates <this ticket id>` and `take_ticket`.
2. Read `src/Pegasus.Web/Pages/Cases/Index.cshtml.cs` and `src/Pegasus.Web/Pages/Cases/Details.cshtml.cs` in full and list, in the ticket research document, every Core port each handler calls. The endpoints call the same ports; no new query is written in `Pegasus.Web`.
3. **Move the four case-identity facts off the intake draft (upstream CASE-020).** In `src/Pegasus.Infrastructure/Persistence/EfCaseQueryStore.cs` `SearchRows` (`:224-252`), stop sourcing `Registration`, `Claimant`, `ClaimNumber` and `InstructionDate` from `InstructionDraftEntity` (`:244-246`, `:248`) and source each from the case's own `CaseDataFields` under the `CaseField.Current` rule `Confirmed ?? Fact ?? Suggestion` (`src/Pegasus.Core/Cases/CaseDataContracts.cs:54`), keeping the receipt join for `ReceivedAtUtc` and `Origin`, which are genuinely intake facts. Leave the draft where it belongs — as intake evidence on the receipt — and change no write path. Done means the four columns resolve from `CaseDataFields` in one composed query, not one query per row: if the set-based projection cannot be expressed, stop and record the alternative in the ticket plan rather than adding a per-row `ICaseDataQueries.GetAsync` call to the list.
4. Re-check the search and sort paths against the moved source, which CASE-020 names as its third scope item: `CaseSearchOrder.RegistrationAsc`/`RegistrationDesc` still order correctly, the `Registration` filter's whitespace-and-hyphen-stripped comparison (`EfCaseQueryStore.cs:50-54`) still matches, and the `Claimant`, `ClaimNumber` and `InstructionDate` filters and the global `Query` term still find the same cases. Any behaviour that changes is recorded in the ticket plan with the reason.
5. Add `src/Pegasus.Contracts/Cases/` response DTOs: `CaseListItemResponse`, `CaseHeaderResponse` (header + overview + `version`), and one response per section named for the section route. Do not expose `CaseDetails` or any Core record directly. `CaseListItemResponse` and `CaseHeaderResponse` carry registration, claimant, claim number and instruction date from the source step 3 establishes — the case's own `CaseDataFields` resolved by `CaseField.Current` — never from `InstructionDraftEntity`, so the list, the header band and the Vehicle and Case identity blocks show one value in three places.
6. Add `src/Pegasus.Web/Api/CaseReadEndpoints.cs` mapping a `cases` sub-group with `.RequireStaffRight(StaffAccessRight.PerformCasework)`. Map `GET /` (list), `GET /{id}` (header+overview) and the seven section routes `/{id}/vehicle`, `/{id}/assessment`, `/{id}/documents`, `/{id}/communications`, `/{id}/tasks`, `/{id}/reports`, `/{id}/history`.
7. Bind the list query parameters onto `CaseSearchFilters` exactly: `stage` → `State`, `assignee` → `EngineerId`, `principal` → `Principal`, `q` → `Query`, plus the date filters the record already carries. Reject any `sort` value that is not a `CaseSearchOrder` name with a 400 `urn:pegasus:problem:validation` problem — that is the sort whitelist from § 3.
8. Default `sort` to `CaseSearchOrder.ReceivedDesc` and `pageSize` to 25 with a hard cap of 200; return `PagedResult<T>` from `src/Pegasus.Contracts`, whose five members are `(Items, Page, PageSize, HasPreviousPage, HasNextPage)` ([[DSK-02-04]] step 3) — it carries no total-count member of any name, because `SearchCasesResult` supplies only `HasPreviousPage`/`HasNextPage`. Do not add one here.
9. Return `version` in the `GET /cases/{id}` body and a weak `ETag` (`W/"<version>"`) per § 3 row *Concurrency*; give each section its own `ETag` and honour `If-None-Match` with 304. `If-Match` is **not** the concurrency mechanism — do not accept it.
10. Make each section route hit only the ports that section needs, so a case header does not pay for documents or assessment. Where the only available port today returns the whole `CaseDetails`, project the section from it but record the N+1/over-fetch in the ticket plan as a follow-up rather than adding a new Core query without a decision.
11. Add `GET /audit` in the same group: `PerformCasework` returns history for a case the caller may see; the unfiltered actor/date search requires `ManageStaffAccounts` per the endpoint-map row. Reuse the action-history / security-event read ports at `src/Pegasus.Core/Identity/IdentityContracts.cs:98-137`.
12. Run the `optimizing-ef-core-queries` skill over the EF implementations behind `ICaseQueryStore` (`src/Pegasus.Infrastructure/Persistence/`) that the list and section routes exercise, including the step 3 projection change, and record the findings in the ticket plan. Done means no new N+1 introduced by the section split or by the moved source.
13. Add `tests/Pegasus.IntegrationTests/DesktopGatewayCaseReadTests.cs` covering: paging (page 1 and 2 boundaries, `HasNextPage`), the newest-first default, each whitelisted `sort` value, each dropdown filter, an invalid `sort` returning a `validation` problem, each section returning independently with its own `ETag`, `If-None-Match` returning 304, and a list/detail comparison against the Razor pages for the same seeded data. Add CASE-020's own verification as a named fact: seed a case whose registration was corrected by staff (the correction writes `CaseDataFields`, not the draft) and assert the corrected value appears in the list row, in `CaseHeaderResponse` and in the vehicle section — one value, three places; and a second fact seeding a case with **no** draft row at all, asserting the list and header show the real registration and claimant rather than the empty values `EfCaseQueryStore` produces today.
14. Regenerate and commit the OpenAPI snapshot (`pwsh ./eng/api/Export-OpenApiDocument.ps1`) and the client (`pwsh ./eng/api/Generate-ApiClient.ps1`). Run the simplification pass and record it under a dated `## Simplification pass` heading in the ticket plan.

## Acceptance conditions

- [ ] Paging, filtering and sorting are server-side, whitelisted, and default to newest received first.
- [ ] The paging envelope reports no total count, because the query port supplies none — `PagedResult<T>` carries exactly `(Items, Page, PageSize, HasPreviousPage, HasNextPage)`.
- [ ] Case detail is sectioned; each section is independently retrievable with its own `ETag`.
- [ ] `GET /cases/{id}` returns `version` in the body and a weak `ETag`.
- [ ] The EF query review records no new N+1 for the list or any section, including the moved case-identity source.
- [ ] `CaseListItemResponse` and `CaseHeaderResponse` source registration, claimant, claim number and instruction date from the case's own `CaseDataFields` (`CaseField.Current` = `Confirmed ?? Fact ?? Suggestion`), never from `InstructionDraftEntity`; a case corrected by staff shows the corrected value in the list, in the header and in the vehicle section — one value, three places (upstream CASE-020).
- [ ] `CaseSearchOrder.RegistrationAsc`/`RegistrationDesc`, the registration filter's stripped comparison, and the claimant, claim-number, instruction-date and global-query filters were re-checked against the moved source and still find the same cases.
- [ ] List and detail figures match the Razor pages for the same seeded data — **except** registration, claimant, claim number and instruction date, which the Razor pages still read from the intake draft. That divergence is the upstream CASE-020 fix landing and is asserted as a deliberate, recorded difference; it is the one stated exception to this criterion, which as previously written enshrined the defect by construction.

## Verification

- [ ] `dotnet test ./tests/Pegasus.IntegrationTests/Pegasus.IntegrationTests.csproj -c Release --filter "FullyQualifiedName~DesktopGatewayCaseReadTests"` — expected: all facts pass, including the newest-first default, the invalid-sort validation problem, the staff-corrected-registration fact and the no-draft-row fact.
- [ ] `grep -n "InstructionDraftEntity" src/Pegasus.Infrastructure/Persistence/EfCaseQueryStore.cs` — expected: no line inside `SearchRows` assigns `Registration`, `Claimant`, `ClaimNumber` or `InstructionDate` from `draft`.
- [ ] `pwsh ./eng/api/Export-OpenApiDocument.ps1 && git diff --exit-code openapi/` — expected: exit 0 after the regenerated snapshot is committed.

## Risks and boundaries

- **Azure**: no write.
- **Scope boundary**: may touch `src/Pegasus.Web/Api/**`, `src/Pegasus.Contracts/Cases/**`, `openapi/`, the generated client and the test projects. **Named exception for upstream CASE-020**: this ticket may also edit the `SearchRows` projection in `src/Pegasus.Infrastructure/Persistence/EfCaseQueryStore.cs` (`:224-252`) and nothing else in `src/Pegasus.Infrastructure` — the fix is a read projection, it is the one place the defect lives, and no other seeded ticket is permitted to make it. Must not modify `src/Pegasus.Web/Pages/Cases/**`, must not change any write path or the intake draft itself, and must not add a new Core query port without a recorded decision.
- **Traps**: do not port `Pages/Cases/CaseMutationPageModel.cs`'s TempData proposed-values/lease chaining — that is web-only state the desktop keeps in memory. Design authority: filters are dropdowns and tables sort newest first (`docs/design/README.md` § No explanatory copy and page economy). Contract changes must stay additive once the pilot ring exists. Upstream `main` is ahead of the fork; check for drift in the case pages after the first upstream sync ([[DSK-01-10]]). Upstream CASE-020 is **latent, not live** — all three production cases currently carry draft rows matching their case fields — so a "nothing changes in production" observation is expected and is not evidence the fix is unnecessary; the failure appears the first time staff correct a case. Whether the header should repeat registration and claimant at all is explicitly out of upstream CASE-020's scope (an open operator decision under **upstream CASE-018**, which has no fork ticket) — do not remove or restructure the header band here. **Upstream ids and fork board ids do not match, and every `CASE-` id in this body collides**: upstream CASE-020 was absorbed here and has **no fork ticket**; upstream CASE-021 is board [[CASE-001]] and upstream CASE-022 is board [[CASE-002]]; upstream `CASE-001` and upstream `CASE-002` are different tickets again and were both dropped, so board `CASE-001`/`CASE-002` never mean them; and upstream CASE-011, CASE-012, CASE-018, CASE-019 and ENG-013 have no fork tickets at all. Always write `upstream <ID>` or `upstream <ID> (board [[<board-id>]])`, never a bare `CASE-0nn`.
- **Simplification pass** (`AGENTS.md` step 4): required over this branch diff before the PR, recorded under a dated `## Simplification pass` heading in the plan document.
