# Checklist — GWY-013: DSK-03-13 · Triage, Unidentified, image-intake and Operations endpoints as explicit named commands

- [ ] Orient. Read the six endpoint rows quoted above in `docs/desktop/03-gateway-api-and-data/endpoint-map.md` § Triage, Unidentified, Operations and the image-intake row in § Intake, and the upstream INTK-035 note under Source of truth. Then `get_doc_gates <this ticket id>` and `take_ticket`.
- [ ] Read `src/Pegasus.Web/Pages/Triage/Details.cshtml.cs` in full — especially the `switch (actionName)` at lines 114–204 — and write, in the ticket research document, a table of the twelve cases: case name → route → Core interface → request record → extra arguments. The endpoint-map row says "verify the full set"; if the code has more than twelve cases after the upstream sync, add the extra routes and note the difference.
- [ ] Read `src/Pegasus.Web/Pages/Unidentified/Details.cshtml.cs`, `ImageIntake/Index.cshtml.cs`, `ImageIntake/Details.cshtml.cs` and `Operations/Index.cshtml.cs` and record the same table for each.
- [ ] Add `src/Pegasus.Contracts/Triage/`, `Unidentified/`, `ImageIntake/` and `Operations/` DTOs. Each triage command DTO carries only the arguments its case actually needs — do not create one fat request with every optional field, which would recreate the dispatcher in a different shape.
- [ ] Add `src/Pegasus.Web/Api/TriageEndpoints.cs` mapping a `triage` sub-group with `.RequireStaffRight(StaffAccessRight.PerformCasework)` and the twelve named command routes plus the list, detail and source-byte reads. Each handler calls the same Core interface its dispatcher case calls.
- [ ] Add `src/Pegasus.Web/Api/UnidentifiedEndpoints.cs`, `ImageIntakeEndpoints.cs` and `OperationsEndpoints.cs` for the remaining groups. Apply the byte-endpoint conventions from [[DSK-03-10]] to `/triage/{id}/source` and `/unidentified/{id}/members/{mid}/source`.
- [ ] Give `POST /unidentified/{id}/resolve` the staff promote path (upstream INTK-035 — absorbed here and in [[DSK-05-12]]; there is no fork ticket for it). Today the route is a bare resolve, so an Unidentified triage request whose registration never gets extracted can never be promoted. Fix the contract and the orchestration together:
- [ ] Resolve the origin-receipt lookup before step 7 can run. Upstream INTK-035 cites `ITriageQueries.GetByOriginReceiptAsync` as existing plumbing; the fork's `ITriageQueries` (`TriageContracts.cs:288-294`) carries only `ListAsync` and `GetAsync`, because that member arrives with the unmerged upstream INTK-033 — imported on this board as upstream INTK-033 (board [[INTK-007]]). After [[DSK-01-10]]'s sync, check whether the member is present. If it is, use it. If it is not, add exactly that one read-only query member to `ITriageQueries` and its EF implementation here under the named scope exception below — this ticket is its single owner; [[DSK-05-12]] must not add it. Record which case applied in `research`, and coordinate with [[INTK-007]] so the member is defined once rather than twice.
- [ ] Allow an operation key up to 200 characters on the unidentified resolve route, matching `src/Pegasus.Core/Intake/Unidentified/UnidentifiedContracts.cs:398`, and 100 everywhere else. Encode the limit in the contract, not as a magic number in the handler.
- [ ] Apply the Engineer-selection rule on `POST /triage/{id}/assign` exactly as the page does (INTK-019); the rule stays in Core, the endpoint only maps the staff id.
- [ ] Make sure the operations retry and revoke commands write the same audit records the page handlers write — they are the paths an auditor uses to explain a re-run, so a missing action-history entry is a defect.
- [ ] Add `tests/Pegasus.IntegrationTests/DesktopGatewayTriageTests.cs` and `DesktopGatewayOperationsTests.cs`: the seven-case matrix for each of the twelve triage commands, the unidentified resolve, the image-intake close and the two operations commands; plus list/detail parity against the Razor pages and the byte-endpoint header facts. Instantiate the resolve route's matrix for the promote path as well, with the validation-failure case being an invalid registration — refused, with no Triage created and the item still open — and add these named promote facts: a supplied registration opens exactly one Triage from the originating receipt and closes the Unidentified item with the resolution recorded; an item whose originating receipt already has a Triage resolves against that existing Triage and never creates a second; a `registration` sent with any `targetKind` other than `Triage` is a validation failure; and a resolve with no `registration` behaves byte-for-byte as it does today.
- [ ] Add one fact per group asserting there is no generic action route: enumerate `EndpointDataSource.Endpoints` and assert no `/api/v1` route pattern ends in `action` or accepts an action-name form field.
- [ ] Regenerate and commit the OpenAPI snapshot and the generated client, run the filtered tests, then run the simplification pass and record it under a dated `## Simplification pass` heading in the ticket plan.
- [ ] Every case in `Pages/Triage/Details.cshtml.cs:116-204` has its own named endpoint; no dispatcher string reaches the API.
- [ ] `POST /api/v1/unidentified/{id}/resolve` carries the full `ResolveUnidentifiedRequest` member set — `expectedVersion`, `operationKey`, `reason`, the required `targetKind`, `targetId` and the optional `targetReference` — plus one new **optional** `registration` accepted only with `targetKind = Triage`; `targetId` is required except in that promote case, where this endpoint derives it. A resolve sent without `registration` is unchanged from today's behaviour (upstream INTK-035).
- [ ] A supplied registration opens exactly one Triage from the originating receipt through `ICreateTriageFromIntake` and closes the Unidentified item with the resolution recorded against `UnidentifiedResolutionTargetKind.Triage`; the registration is normalised only by `ImageIntakeLifecycle.NormalizeRegistrationInput` and judged only by `TriageLifecycleRules.ValidateCreate`, with no second normaliser or validator anywhere in the gateway; an originating receipt that already has a Triage never gains a second; and a replay of the same `operationKey` creates nothing new.
- [ ] The resolve route's seven-case matrix is instantiated for the promote path, its validation-failure case being an invalid registration that creates no Triage and leaves the item open.
- [ ] Unidentified resolve accepts a 200-character operation key; every other command caps at 100.
- [ ] Operations retry and revoke are audited exactly as the page handlers audit them.
- [ ] Byte reads carry `Content-Length`, `ETag`, range, `nosniff` and a safe filename.
- [ ] Every command has the seven-case test matrix.
- [ ] `dotnet test ./tests/Pegasus.IntegrationTests/Pegasus.IntegrationTests.csproj -c Release --filter "FullyQualifiedName~DesktopGatewayTriageTests"` — expected: all facts pass, including the no-dispatcher assertion and the four promote facts (one Triage from the originating receipt, invalid registration refused, already-has-a-Triage creates no second, no-`registration` resolve unchanged).
- [ ] `dotnet test ./tests/Pegasus.IntegrationTests/Pegasus.IntegrationTests.csproj -c Release --filter "FullyQualifiedName~TriageQueuesWebTests"` — expected: the existing triage web tests still pass unchanged.
- [ ] `dotnet test ./tests/Pegasus.IntegrationTests/Pegasus.IntegrationTests.csproj -c Release --filter "FullyQualifiedName~UnidentifiedPersistenceTests"` — expected: unchanged and passing; the existing resolve path is untouched by the added optional member.

## Progress notes

No implementation has started. This checklist is derived from the ticket’s accepted scope and is maintained by the ticket implementer.
