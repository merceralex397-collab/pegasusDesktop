# Checklist — GWY-001

One box per plan step, in plan order. The last box produces `proof`.

- [ ] Read `docs/desktop/03-gateway-api-and-data/README.md:163-169` and § 7, `endpoint-map.md` § Conventions, and [[FND-029]]'s (plan handle `DSK-02-04`) `plan` document via `get_ticket_doc FND-029 plan`; load `.agents/skills/project/pegasus-desktop/SKILL.md`; call `get_doc_gates GWY-001`; `take_ticket` on branch `task/contracts-conventions`, worktree `../pegasus-worktrees/contracts-conventions`, from `origin/dev`.
- [ ] Confirm `src/Pegasus.Contracts/Pegasus.Contracts.csproj` exists, targets `net10.0` and is listed in `Pegasus.slnx`; `ls` the five envelope files; record in the plan document which branch applies (extend-in-place vs create-the-missing). Stop and record a blocker if the csproj is absent — never create a second Contracts project.
- [ ] `ProblemDetails/PegasusProblemTypes.cs`: verify `Prefix = "urn:pegasus:problem:"` and exactly the thirteen slugs from `README.md:167`, changing no existing member — or create it to that shape if absent. Record which case applied.
- [ ] `ProblemDetails/PegasusProblem.cs`: verify or create the RFC 9457 members plus `CorrelationId` and the typed `CurrentVersion`/`MinimumVersion` accessors; add XML documentation of the no-payload-dump boundary rule quoting `src/Pegasus.Web/Mcp/AutomationMcpErrors.cs:7-15`, and of why `CurrentVersion` is typed (`CaseWorkflowContracts.cs:125-149`). Record which case applied.
- [ ] `Paging/PagedResult.cs` and `Paging/PagingLimits.cs`: verify the five-member record and `MaxPageSize = 200` unchanged — or create them to that shape; add the XML-doc note on `PagingLimits` that a per-endpoint cap may be lower, citing `ListIntake` at `src/Pegasus.Core/Intake/IntakeQueryUseCases.cs:22-27`. Record which case applied.
- [ ] `Requests/MutationEnvelope.cs`: verify or create `ExpectedVersion`/`OperationKey`/`Reason`/`EditLeaseToken` as body fields with `OperationKeys.MaxLength = 100` and the `desk:` prefix; add XML documentation of the `desk:<guid>` format under `RequireOperationKey`'s constraints (`AutomationMcpErrors.cs:76-89`) and the single 200-character exception (`UnidentifiedContracts.cs:398`). Do not re-implement the validation. Record which case applied.
- [ ] Create `src/Pegasus.Contracts/ContractConventions.cs` in the shape of `src/Pegasus.Core/CoreAssembly.cs`, with the four conventions (DTO suffixes; Core records never exposed directly; enums as strings; `DateTimeOffset` UTC) as XML documentation, plus the sentence recording that it is the stable assembly marker the architecture fact asserts against. No new `.md` file.
- [ ] Extend the single `ContractsProjectHasNoDependencies` fact in `tests/Pegasus.ArchitectureTests/DependencyDirectionTests.cs` with an `Assert.DoesNotContain` assembly-reference assertion over `typeof(ContractConventions).Assembly.GetReferencedAssemblies()` against a Contracts-specific eight-prefix array used through the `IsForbiddenCoreDependency` (`:475-478`) matcher shape; leave the csproj-XML assertions and `ForbiddenCoreDependencyPrefixes` (`:23-39`) untouched; confirm the test project references `Pegasus.Contracts`.
- [ ] Run `dotnet build Pegasus.slnx -c Release` and confirm `0 Warning(s)` `0 Error(s)`.
- [ ] Run `dotnet test ./tests/Pegasus.ArchitectureTests/Pegasus.ArchitectureTests.csproj -c Release` unfiltered and confirm the extended fact passes with no regression in `CoreHasNoInfrastructureOrHostDependencies` or `ApplicationSolutionExcludesSourceWorkspaces`.
- [ ] Record in the plan document which branch step 2 found and which case applied per envelope file; run the simplification pass over the branch diff and record findings and dispositions under a dated `## Simplification pass` heading.
- [ ] **Verification run (this box produces `proof`)** — capture, as tier-1 static/build/architecture evidence: the `dotnet build` summary showing `0 Warning(s)`; the unfiltered `dotnet test` output; `ls src/Pegasus.Contracts/Problems src/Pegasus.Contracts/Commands` (both absent); `grep -n 'record PagedResult' src/Pegasus.Contracts/Paging/PagedResult.cs` (one line, five members); `grep -rn 'Total' src/Pegasus.Contracts/Paging/` (no matches); `grep -rn 'ActionActor' src/Pegasus.Contracts/` (no matches); and `grep -rn 'ContractsHasNoInfrastructureOrHostDependencies\|ProblemTypes\b\|CommandEnvelope' tests/ src/` (no matches). Claim no endpoint behaviour.

## Progress notes

(append with `set_ticket_doc(doc: "checklist", append: true)`)
