---
id: GWY-001
type: ticket
title: >-
  DSK-03-01 · `Pegasus.Contracts`: DTO conventions, problem types, paging
  envelope, version/operation key
status: done
area: gateway-api
order: 240
assignee: codex-mcp-client
profile: feature
stageEntered:
  preparing: '2026-08-24T21:23:45.003Z'
  review: '2026-08-26T22:41:33.541Z'
  verifying: '2026-08-26T23:10:06.174Z'
  done: '2026-08-26T23:12:16.310Z'
taken_at: '2026-08-26T22:34:14.409Z'
branch: task/gwy-001-contract-conventions
worktree: ../pegasus-worktrees/gwy-001-contract-conventions
labels:
  - desktop-conversion
  - plan-03
  - phase-2
  - tier-1
groups:
  - EPIC-004
  - HZN-003
links: []
blocks:
  - GWY-002
  - GWY-016
  - DUI-005
  - TEST-001
  - PLAT-024
docs_todo: true
archived: false
created: '2026-08-24T07:46:20.085Z'
updated: '2026-08-27T00:46:49.322Z'
---

## What

Establish the shared contract vocabulary inside `src/Pegasus.Contracts` (the project `DSK-02-04` scaffolds): DTO naming and shape conventions, the RFC 9457 problem-type catalogue, the paging envelope, and the explicit `expectedVersion` / `editLeaseToken` / `operationKey` fields every `/api/v1` command body carries — plus an architecture test that keeps the project free of EF Core, ASP.NET Core and WinUI references.

## Why

Proposal § 10.3 makes the OpenAPI document the contract and forbids hand-written duplicate DTOs; § 10.4 requires an explicit concurrency token on every mutable aggregate. Without one contracts project, the desktop and the gateway grow separate record shapes and separate error vocabularies, breaking the "one list per concept" rail in `AGENTS.md` § Simplicity rails. Operator-visible consequence: a version conflict, a lease expiry and a validation refusal reach the desktop as three distinct stable `type` URIs, so the client can offer reload, reacquire or correct instead of one generic failure. Every later ticket in this epic — [[DSK-03-02]] through [[DSK-03-18]] — compiles against what this ticket defines.

## Source of truth

- Plan row: `docs/desktop/03-gateway-api-and-data/README.md` § 5 — `DSK-03-01`
- Plan detail: `docs/desktop/03-gateway-api-and-data/README.md` § 3 — rows *Contracts*, *Idempotency*, *Concurrency*, *Problem details*, *Paging/filter/sort*
- Plan detail: `docs/desktop/03-gateway-api-and-data/endpoint-map.md` § Conventions
- Proposal: `docs/desktop/Pegasus_Native_Desktop_Design_Proposal.md` § 10.2 API style, § 10.3 Generated client, § 10.4 Concurrency
- Repository evidence:
  - `src/Pegasus.Core/Workflow/CaseWorkflowContracts.cs:182` — `CaseMutationRequest(Guid CaseId, long ExpectedVersion, ActionActor Actor, string OperationKey, string Reason, string EditLeaseToken)`, the shape the DTOs mirror
  - `src/Pegasus.Core/Workflow/CaseWorkflowContracts.cs:125`, `:135`, `:143`, `:151` — `CaseVersionConflictException`, `CaseEditLeaseConflictException`, `CaseEditLeaseExpiredException`, `CaseOperationConflictException`
  - `src/Pegasus.Web/Mcp/AutomationMcpErrors.cs:19-76` — the existing exception-to-transport mapping this catalogue ports
  - `src/Pegasus.Web/Mcp/AutomationMcpErrors.cs:76` — `RequireOperationKey`: `mcp:` prefix, ≤ 100 characters, no whitespace or control characters
  - `src/Pegasus.Core/Cases/CaseQueries.cs:69` — `SearchCasesResult(Items, Page, PageSize, HasPreviousPage, HasNextPage)`: no total count exists, so the envelope must not promise one
  - `src/Pegasus.Infrastructure/Persistence/EfCaseQueryStore.cs:114-132` — the fetch-one-extra implementation behind those flags (`.Take(query.PageSize + 1)`, `hasNextPage = page.Length > query.PageSize`, `HasPreviousPage = query.Page > 1`), with no `CountAsync`
  - `src/Pegasus.Core/Cases/OrganizationAdministration.cs:47-52`, `src/Pegasus.Core/Identity/AutomationActivity.cs:44-50`, `src/Pegasus.Core/Identity/StaffAccountAdministration.cs:16-21` — the other three paged ports, all the same five-member cursor shape and none of them counting
  - `tests/Pegasus.ArchitectureTests/DependencyDirectionTests.cs:23-40` — `ForbiddenCoreDependencyPrefixes` and the assembly-reference guard to extend
  - `Directory.Build.props:7-8` — `AnalysisLevel=latest-recommended`, `TreatWarningsAsErrors=true`
- Binding decisions:
  - L-01 — the gateway is `Pegasus.Web` evolved in place; contracts are a class library it and the desktop share, not a new deployment unit.
  - L-04 — every ticket names its subagent, skills and MCP tools.
- Depends on: `DSK-02-02` central package management must exist before a new project can reference packages; `DSK-02-04` creates `src/Pegasus.Contracts` and adds it to `Pegasus.slnx`, and **owns every envelope type in `src/Pegasus.Contracts`** — `Paging/PagedResult.cs` with `PagingLimits`, `ProblemDetails/PegasusProblemTypes.cs`, `ProblemDetails/PegasusProblem.cs` and `Requests/MutationEnvelope.cs`; this ticket extends them and adds the conventions marker type.

## Routing

- **Subagent**: `pegasus-gateway-dev` — `.codex/agents/pegasus-gateway-dev.toml`
- **Skills**, loaded in this order: `pegasus-desktop` (`.agents/skills/project/pegasus-desktop/SKILL.md`) → `dotnet-webapi` (`dotnet/skills` `98f84851`, plugin `dotnet-aspnetcore`, `plugins/dotnet-aspnetcore/skills/dotnet-webapi/SKILL.md`) → `microsoft-code-reference` (Microsoft Learn plugin)
- **MCP**: Kanmer (`get_status`, `get_doc_gates`, `take_ticket`, `set_ticket_doc`, `append_scratch`, `move_item`); Microsoft Learn (`microsoft_docs_search`, `microsoft_docs_fetch`)
- **Kanmer pipeline** for profile `feature`: `kanmer-research` → `kanmer-plan` → `kanmer-execute` → `kanmer-review` → `kanmer-verify` → `kanmer-closeout` (call `get_doc_gates <id>` before every move; a move crosses at most one gated boundary)
- **Reviewer**: `pegasus-desktop-reviewer` — an agent that did not implement (`AGENTS.md` § Repository task workflow step 5)

## Implementation steps

1. Orient. Read `docs/desktop/03-gateway-api-and-data/README.md` § 3 and § 7, then `endpoint-map.md` § Conventions. Load `.agents/skills/project/pegasus-desktop/SKILL.md`. Call Kanmer `get_doc_gates <this ticket id>`, then `take_ticket` with the real branch `task/<slug>` and worktree `../pegasus-worktrees/<slug>` created from `origin/dev` (`AGENTS.md` § Repository task workflow steps 1–2).
2. Confirm `src/Pegasus.Contracts/Pegasus.Contracts.csproj` exists from `DSK-02-04`, targets `net10.0`, and is listed in `Pegasus.slnx`. If it is not there yet, stop and record the blocker in the ticket rather than creating a second project.
3. Check whether `src/Pegasus.Contracts/ProblemDetails/PegasusProblemTypes.cs` already exists from [[DSK-02-04]], which owns it. If it does, extend it in place and change no existing member; if it has not landed, create it with exactly the shape [[DSK-02-04]] step 4 pins — `public const string Prefix = "urn:pegasus:problem:";` and one `const string` per slug, exactly this catalogue and no other: `validation`, `not-authorized`, `version-conflict`, `lease-conflict`, `lease-expired`, `operation-conflict`, `client-unsupported`, `password-change-required`, `account-disabled`, `provider-unavailable`, `not-found`, `rate-limited`, `maintenance` (`docs/desktop/03-gateway-api-and-data/README.md` § 3 row *Problem details*) — and record in the plan document which case applied. Add nothing that is not on that list; a fourteenth slug needs a plan change first. There is one literal path and one type name for this concept: never `Problems/ProblemTypes.cs` and never a type called `ProblemTypes`.
4. Check whether `src/Pegasus.Contracts/ProblemDetails/PegasusProblem.cs` already exists from [[DSK-02-04]], which owns it. If it does, extend it in place and change no existing member; if it has not landed, create it with exactly the shape [[DSK-02-04]] step 5 pins — a record carrying the RFC 9457 members `Type`, `Title`, `Status`, `Detail` and `Instance`, plus `CorrelationId` (always populated) and an optional `Extensions` dictionary, with `CurrentVersion` and `MinimumVersion` as typed extension accessors because the `version-conflict` and `client-unsupported` problems carry them — and record in the plan document which case applied. Either way, XML-document that the body never carries payload dumps or infrastructure detail — the rule `AutomationMcpErrors.cs` already enforces for MCP. There is one literal path for this concept: never `Problems/PegasusProblem.cs`.
5. Check whether `src/Pegasus.Contracts/Paging/PagedResult.cs` already exists from [[DSK-02-04]], which owns it. If it does, extend it in place and change no existing member; if it has not landed, create it with exactly the shape [[DSK-02-04]] step 3 pins — `public sealed record PagedResult<T>(IReadOnlyList<T> Items, int Page, int PageSize, bool HasPreviousPage, bool HasNextPage)`, exactly those five members, in that order, and no other, with the page-size cap expressed as `public const int MaxPageSize = 200;` on a `PagingLimits` static class in the same folder — and record in the plan document which case applied. **Why this shape and not a total count**: it is the only shape the server can actually produce. Every paged read port in the repository is the fetch-one-extra cursor shape and none of them counts — `SearchCasesResult` (`src/Pegasus.Core/Cases/CaseQueries.cs:69`), `OrganizationListPage` (`src/Pegasus.Core/Cases/OrganizationAdministration.cs:47-52`), `ListAutomationActivityResult` (`src/Pegasus.Core/Identity/AutomationActivity.cs:44-50`) and `ListStaffAccountsResult` (`src/Pegasus.Core/Identity/StaffAccountAdministration.cs:16-21`) — and the implementation behind them (`src/Pegasus.Infrastructure/Persistence/EfCaseQueryStore.cs:114-132`) reaches the flags by `.Take(query.PageSize + 1)` with `hasNextPage = page.Length > query.PageSize` and `HasPreviousPage = query.Page > 1`, issuing no `CountAsync`. An `int? Total` member would therefore be null on every endpoint this gateway exposes, or would oblige a second COUNT query per page that no port performs — so the envelope does not promise one. Adding a total is a plan change to area 03 § 3 *Paging/filter/sort* and a new Core port, never a field this ticket adds. [[DSK-02-04]] step 3 states this identical five-member list; the two statements must stay word for word the same. Never declare a second paging record. Note in XML docs that a per-endpoint cap may be lower than `PagingLimits.MaxPageSize` — `ListIntake` at `src/Pegasus.Core/Intake/IntakeQueryUseCases.cs:24` refuses a page size above 100.
6. Check whether `src/Pegasus.Contracts/Requests/MutationEnvelope.cs` already exists from [[DSK-02-04]], which owns it. If it does, extend it in place and change no existing member; if it has not landed, create it with exactly the shape [[DSK-02-04]] step 6 pins — `MutationEnvelope` mirroring `CaseMutationRequest` minus the server-only actor: `long ExpectedVersion`, `string OperationKey`, `string Reason`, `string EditLeaseToken`, with `OperationKeys.MaxLength = 100` and the desktop prefix `desk:` as constants, and `ActionActor` never copied into Contracts — and record in the plan document which case applied. There is one literal path and one type name for this concept: never `Commands/CommandEnvelope.cs` and never a type called `CommandEnvelope`. Either way, confirm the concurrency and idempotency members are carried as explicit body fields, never headers, and document the desktop key format `desk:<guid>` with the same constraints `RequireOperationKey` enforces (≤ 100 characters, no whitespace, no control characters; 200 only where Core allows, e.g. `src/Pegasus.Core/Intake/Unidentified/UnidentifiedContracts.cs:398`).
7. Write the conventions as a doc comment on a `ContractConventions` marker type in the project (not a new `.md` file — a Markdown file outside `docs/(prd|frd|adr|design|desktop)` fails the CI `documentation` job): DTO suffixes `Request`/`Response`, no Core record is exposed directly (they carry `ActionActor` and server-only members), enum values serialise as strings, dates are `DateTimeOffset` in UTC.
8. Extend the `ContractsProjectHasNoDependencies` fact [[DSK-02-04]] step 11 adds to `tests/Pegasus.ArchitectureTests/DependencyDirectionTests.cs` rather than adding a second matcher: leave its existing assertion that the project declares no `PackageReference`, no `ProjectReference` and no `FrameworkReference` untouched, and add to that same fact the assembly-reference assertion that `Pegasus.Contracts` references none of `Microsoft.AspNetCore*`, `Microsoft.EntityFrameworkCore*`, `Microsoft.WindowsAppSDK`/`Microsoft.UI.Xaml`, `Pegasus.Core`, `Pegasus.Infrastructure`, `Pegasus.Web`, `Pegasus.Worker` — reusing the existing `IsForbiddenCoreDependency` helper shape. If [[DSK-02-04]] has not landed, add the fact under that one name and record which case applied. Never add a second matcher beside it, and never under the name `ContractsHasNoInfrastructureOrHostDependencies`.
9. Run `dotnet build Pegasus.slnx -c Release`. Done means zero warnings: `TreatWarningsAsErrors=true` is on, so an analyzer warning is a build break, and lowering the policy is not an option (`README.md` § 7).
10. Run `dotnet test ./tests/Pegasus.ArchitectureTests/Pegasus.ArchitectureTests.csproj -c Release` and confirm the new fact passes and no existing fact regressed.
11. Run the simplification pass over the branch diff (`AGENTS.md` step 4) and record findings and dispositions under a dated `## Simplification pass` heading in the ticket plan document.

## Acceptance criteria

- [ ] `src/Pegasus.Contracts` builds as part of `Pegasus.slnx` with no warnings.
- [ ] The thirteen problem-type slugs from the plan exist as constants, spelled exactly as the plan lists them, and nothing else.
- [ ] `PagedResult<T>` carries exactly `(IReadOnlyList<T> Items, int Page, int PageSize, bool HasPreviousPage, bool HasNextPage)` — five members, no total-count member of any name — matching the identical list in [[DSK-02-04]] step 3, because no Core paging port produces a count.
- [ ] `MutationEnvelope` exposes `operationKey`, `expectedVersion` and `editLeaseToken` as body fields, not headers.
- [ ] Exactly one file exists per envelope concept: `Paging/PagedResult.cs`, `ProblemDetails/PegasusProblemTypes.cs`, `ProblemDetails/PegasusProblem.cs`, `Requests/MutationEnvelope.cs` — no `Problems/` or `Commands/` folder and no `ProblemTypes` or `CommandEnvelope` type.
- [ ] An architecture test fails if `Pegasus.Contracts` gains an EF Core, ASP.NET Core, WinUI or `Pegasus.*` reference, and it is the single extended `ContractsProjectHasNoDependencies` fact rather than a second matcher.

## Verification

- [ ] `dotnet build Pegasus.slnx -c Release` — expected: `Build succeeded` with `0 Warning(s)` `0 Error(s)`.
- [ ] `dotnet test ./tests/Pegasus.ArchitectureTests/Pegasus.ArchitectureTests.csproj -c Release` — expected: all facts pass, including the extended `ContractsProjectHasNoDependencies`.
- [ ] `ls src/Pegasus.Contracts/Problems src/Pegasus.Contracts/Commands` — expected: both paths absent; the envelope types live under `ProblemDetails/`, `Paging/` and `Requests/` only.
- [ ] `grep -n 'record PagedResult' src/Pegasus.Contracts/Paging/PagedResult.cs` — expected: one line, `public sealed record PagedResult<T>(IReadOnlyList<T> Items, int Page, int PageSize, bool HasPreviousPage, bool HasNextPage)`, and `grep -rn 'Total' src/Pegasus.Contracts/Paging/` returns no matches.

## Evidence tier

Tier 1 — Static/build/architecture. Obliges compiling the approved projects and enforcing dependency direction; it proves consistency only, so no endpoint behaviour may be claimed from this ticket.

## Documentation changes

- None. The conventions live as XML documentation in the project itself; ADR-0101 and ADR-0103 are authored by `DSK-00-05` and area 00, and `docs/index.md` gains the OpenAPI location in [[DSK-03-04]].

## Guardrails

- **Azure**: no write.
- **Scope boundary**: may touch `src/Pegasus.Contracts` and `tests/Pegasus.ArchitectureTests` only, and inside `src/Pegasus.Contracts` it does **not** own the envelope types — [[DSK-02-04]] owns `Paging/PagedResult.cs` and `PagingLimits`, `ProblemDetails/PegasusProblemTypes.cs`, `ProblemDetails/PegasusProblem.cs` and `Requests/MutationEnvelope.cs`, and this ticket extends them in place under steps 3–6. What this ticket owns outright is the `ContractConventions` marker type. Must not touch `src/Pegasus.Web`, `src/Pegasus.Core`, `src/Pegasus.Infrastructure` or any desktop project — endpoints arrive in [[DSK-03-02]].
- **Traps**: `TreatWarningsAsErrors=true` with `AnalysisLevel=latest-recommended` means any analyzer warning breaks the build; never lower the policy. `Pegasus.Contracts` must stay `net10.0` and platform-neutral because `Pegasus.Web` still publishes `linux-x64` into the Playwright base image. Any new `.md` outside `docs/(prd|frd|adr|design|desktop)` fails the CI `documentation` job. Overlaps [[DSK-02-04]], which creates the same envelope types in the same project: one type, one owner — [[DSK-02-04]] owns `PagedResult<T>`/`PagingLimits`, `PegasusProblemTypes`, `PegasusProblem` and `MutationEnvelope`, so steps 3–6 check first and extend in place, and neither ticket ever creates a second copy under a different folder or a different type name. **One `PagedResult<T>` member list** — step 5 above and [[DSK-02-04]] step 3 state the same five members `(Items, Page, PageSize, HasPreviousPage, HasNextPage)`; adding, renaming or removing a member on one side without the other, and in particular reintroducing a total-count member no Core port can populate, is a stop condition. The architecture fact runs the same way — [[DSK-02-04]] step 11 owns `ContractsProjectHasNoDependencies` and step 8 extends that one fact rather than adding a second matcher.
- **Simplification pass** (`AGENTS.md` step 4): required over this branch diff before the PR, recorded under a dated `## Simplification pass` heading in the plan document.

## Outcome

_Filled at closeout._
