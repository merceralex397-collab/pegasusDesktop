2026-08-27 — Exact Automation ingress rerun still fails before handler execution with OpenIddict missing encryption key. Official/local 7.6.0 evidence shows UseDataProtection changes payload formats but server validation still requires encryption and asymmetric signing credentials; authorization-code + PKCE remains enabled. Repository search found no approved durable credential source. No ephemeral/development credential or cloud/credential write will be introduced. Implementation is paused at this blocker; next action is an approved credential path or a product decision on the existing authorization-code capability.

2026-08-30 live handoff: the ticket's existing plan records the exact blocker from the extracted-composition validation. With Data Protection enabled, OpenIddict startup still rejects the retained Automation authorization-code/PKCE capability unless encryption and asymmetric signing credentials are registered. The repo has no approved durable credential source for this branch; adding ephemeral/dev credentials would violate the acceptance and security boundary, and removing/defering the Automation capability is a product decision not guessed here. Current exact MSIX identity/publisher values are unrelated and do not unblock this token-flow issue. No code changed in this claim; release in favor of an independent ticket. Smallest unblock: operator-approved in-repo/runtime credential source for retained Automation authorization-code capability, or explicit decision to remove/defer that capability.
