# Files — GWY-009: Case tasks, notes, manual chasers and report-evidence link endpoints

Paths marked **(created by …)** do not exist on disk today (`ls src` returns `Pegasus.Core`,
`Pegasus.Infrastructure`, `Pegasus.Web`, `Pegasus.Worker` and nothing else). Confirm the named
ticket has merged before starting; this ticket must not create those projects itself.

## Where the change lands

| Path | New/edit | What happens here |
| --- | --- | --- |
| `src/Pegasus.Contracts/Cases/Commands/CaseTaskCommands.cs` | **new** (project created by [[GWY-001]], plan handle `DSK-03-01`) | The four task DTOs. `CreateCaseTaskRequestDto` carries `expectedVersion` + `operationKey` + `reason` + `editLeaseToken` + `description` + `assigneeId` and **no** task version; `AssignCaseTaskRequestDto`, `CompleteCaseTaskRequestDto`, `CancelCaseTaskRequestDto` each carry `expectedVersion` **and** `taskExpectedVersion`. Plus the `CaseTaskResponseDto` mirroring `CaseTaskRecord` (id, caseId, description, assigneeId, state, `version`, `caseVersion`) |
| `src/Pegasus.Contracts/Cases/Commands/CaseNoteAndChaseCommands.cs` | **new** (same project) | `AddCaseNoteRequestDto` (`operationKey`, `note` — **no** version, **no** lease token, per CASE-017); `RecordManualChaseRequestDto` (case `expectedVersion`, `editLeaseToken`, `operationKey`, `reason`, `channel`, `targetPartyOrAddress`, `attemptedAtUtc`, `outcome`, `note?`); `ReportEvidenceLinkRequestDto` (case `expectedVersion`, `editLeaseToken`, `operationKey`, `reason`, `evidenceId`) used by both link and unlink; the `AddCaseNoteResponseDto` (note id + case version) and `CaseVersionResponseDto` |
| `src/Pegasus.Web/Api/CaseTaskEndpoints.cs` | **new** (folder created by [[GWY-002]], plan handle `DSK-03-02`) | The eight named routes, mapped onto the `cases` sub-group from [[GWY-008]] (plan handle `DSK-03-08`). Each handler: read the actor from `HttpContext.Items`, build the Core request record, await the Core port, project the result. No `try`/`catch`, no rule |
| the `/api/v1` problem-details mapper | **edit** (created by [[GWY-002]]) | One new arm: `CaseTaskVersionConflictException` → `urn:pegasus:problem:version-conflict`, 409, carrying `taskId`, `expectedVersion`, `currentVersion` from the exception's own properties (`src/Pegasus.Core/Tasks/CaseTaskContracts.cs:21-30`). Step 6 of the body forbids a second catch block in the endpoint file |
| the `cases` sub-group registration | **edit** (created by [[GWY-008]]) | One call adding this file's routes to the group, so the group's auth filter, write rate limiter and correlation middleware apply without repetition |
| `src/Pegasus.Infrastructure/Persistence/EfDocumentCustodyStore.cs` | **edit**, `:419-461` only | `ILogicallyRemoveDocument.ExecuteAsync` gains one `context.CaseWorkflowEvents.Add(…)` before `SaveChangesAsync` at `:459`. Today it writes only the four version flags at `:453-456` and no event at all |
| `src/Pegasus.Infrastructure/Persistence/EfQueuedCustodyProcessor.cs` | **edit**, `:594-605` and `:661` only | Two writes move from `CaseHistory` to `CaseWorkflowEvents`: `custody_confirmed` (currently `context.Set<CaseHistoryEntity>().Add`, `:594`) and `audit_custody_confirmed` (currently `context.CaseHistory.Add`, `:661`). **Moved, not duplicated** |
| `src/Pegasus.Infrastructure/Persistence/EfExternalWorkStore.cs` | **edit**, `:450-456`, `:481-487`, `:608-614`, `:635-641` only | Four writes move the same way: `custody_failed` at `:450` and `:608`, `audit_custody_failed` at `:481` and `:635`. The fifth `CaseHistory.Add` in this file, at `:210`, is a **different event and is out of scope** |
| `tests/Pegasus.IntegrationTests/DesktopGatewayCaseTaskTests.cs` | **new** | The seven-case matrix × eight commands, the wrong-version-scope fact, and the seven DOCS-012 round-trip facts plus the `CaseHistory`-gains-no-row negative |
| `openapi/pegasus-v1.json` | **edit** (created by [[GWY-004]], plan handle `DSK-03-04`) | Regenerated: eight paths, ten request schemas, three response schemas. Snapshot-tested, so an unreviewed change fails CI |
| `src/Pegasus.Desktop.Infrastructure/Api/Generated/**` | **edit** (created by [[FND-031]], plan handle `DSK-02-06`; generator added by [[GWY-005]], plan handle `DSK-03-05`) | Kiota output regenerated by `eng/api/Generate-ApiClient.ps1`. Committed; CI fails if regeneration is not a no-op |

## Context files

Read these before writing a line. Each one is here for the specific thing it tells you.

| Path | What it tells you |
| --- | --- |
| `src/Pegasus.Web/Pages/Cases/Tasks.cshtml.cs` (248 lines) | The exact Core call and argument order for all eight commands, at `:33`, `:61`, `:89`, `:117`, `:143`, `:169`, `:201`, `:225`. It is the specification: your endpoint's job is to build the same record from a JSON body instead of a form post. **Do not edit it** — the guardrail forbids it, and it must keep working through coexistence |
| `src/Pegasus.Web/Pages/Cases/Tasks.cshtml.cs:28-32` | The CASE-017 decision, in the repository's own words: a note takes no edit lease and no expected version, "so it must not contend with an engineer editing the same case". This is why the notes DTO has two fields and not six, and why the endpoint-map row saying "`CaseMutationRequest` fields" is stale text you must not implement |
| `src/Pegasus.Core/Tasks/CaseTaskContracts.cs` | That the four task commands are **not** uniform: `CreateCaseTaskRequest` (`:36-45`) has `ExpectedCaseVersion` only; `AssignCaseTaskRequest`/`CompleteCaseTaskRequest`/`CancelCaseTaskRequest` (`:57-109`) derive from `ExistingCaseTaskMutationRequest` (`:47-55`) and carry both versions. Also that `CaseTaskVersionConflictException` (`:21-30`) is an `InvalidOperationException` subclass — so an unmapped arm silently returns the wrong problem type — and that `CaseTaskRecord` (`:12-19`) already returns both `Version` and `CaseVersion`, so a command response needs no re-read |
| `src/Pegasus.Core/Cases/CaseNotes.cs` | The three rules the note endpoint must **not** duplicate: `PerformCasework` required (`:48`), non-Staff actors barred (`:53-56`, so the Automation actor cannot author a note), 2,000-character cap (`:42`, `:70-75`). Also `AddCaseNote.EventType = "operator_note"` (`:40`) |
| `src/Pegasus.Web/Pages/Cases/CaseMutationPageModel.cs` (339 lines) | The half you are **replacing, not porting**: `ExecuteCaseCommandAsync` at `:109`, actor resolution at `:149`, the `StaffAuthorizationException` catch at `:160`, and the TempData/PRG chaining the area plan § 7 names as a trap. The desktop sends explicit fields; none of this state machine crosses into `/api/v1` |
| `src/Pegasus.Infrastructure/Persistence/EfCaseNoteStore.cs` (66 lines) | The template for every event this ticket writes, and the reason it must be `CaseWorkflowEventEntity`: its class comment at `:13-18` records the Release 22 defect verbatim — "The note was persisted, the page reported success, and the timeline stayed empty." The field set to copy is `:48-63`; the replay guard to copy is `:35-44` (`AnyAsync` on `CaseId` + `OperationKey`) |
| `src/Pegasus.Infrastructure/Persistence/EfCaseQueryStore.cs:181-196` | The history projection your tests assert through — `CaseWorkflowEvents` only, ordered newest-first, `Take(200)`, projected into `CaseHistoryEntry`. It reads nothing from `CaseHistory`, which is why moving the writes is the fix. The `Take(200)` cap is why a test asserts presence of the new entry rather than a total count |
| `src/Pegasus.Infrastructure/Persistence/EfCaseWorkflowStore.cs:901-921` | The existing **system-written** automatic event, `report_evidence_auto_linked`: before/after JSON, version increment, `ClearLease`, then `AddEvent(...)` at `:909-921`. The new custody notes copy this shape. Its replay arm at `:855-872` shows how a replayed operation key is recognised by `RequestHash` |
| `src/Pegasus.Web/Presentation/OperatorLabels.cs:389-394` | The five labels that already exist and must not be reworded: "Sent report linked automatically", "Audit evidence confirmed", "Audit evidence stored", "Audit evidence storage failed", "Document stored", "Document storage failed". Four become reachable for the first time when the writes move. If you find yourself wanting to add a label, the event type you invented is wrong |
| `src/Pegasus.Core/Documents/DocumentContracts.cs:146-152`, `:206-210` | `LogicallyRemoveDocumentCommand` and `ILogicallyRemoveDocument` — that the removal already carries `Reason`, `OperationKey`, `ExpectedCaseVersion` and `EditLeaseToken`, so the new event has an operator-supplied reason and a replay key without any contract change |
| `src/Pegasus.Infrastructure/Persistence/Migrations/20260729199000_RuntimeRoleReconciliation.cs` | That `("CaseWorkflowEvents", "SELECT, INSERT")` is already granted to the **Web** role (`WebGrants`, `:94` list, entry `:122`) **and** the **Worker** role (`WorkerGrants`, `:166` list, entry `:181`). The area plan § 7 names missing runtime grants as the "works locally, fails only in production" trap that has shipped three times; here it does not apply, and this file is the evidence |
| `tests/Pegasus.IntegrationTests/AzureSqlRuntimeRoleMigrationTests.cs:119`, `:177` | The pinned grant census already records `CaseWorkflowEvents:SELECT,INSERT` for both roles — so no census edit either. Read it before concluding you need a migration |
| `tests/Pegasus.IntegrationTests/CaseTasksWebTests.cs` (181 lines) | The Razor behaviour that must stay green after the persistence change, and the fixture shape (`WebApplicationFactory<Program>`, LocalDB per test database) your new test file reuses |
| `tests/Pegasus.IntegrationTests/CaseTaskArchivePersistenceTests.cs` (1,019 lines) | How the repository already writes task version-conflict and replay scenarios — the closest existing model for the seven-case matrix, and the largest source of reusable arrange helpers |
| `tests/Pegasus.IntegrationTests/CaseNotePersistenceTests.cs` (154 lines) | The note round trip asserted the right way; the closest existing model for the DOCS-012 facts |
| `tests/Pegasus.IntegrationTests/DueChaserSweepPersistenceTests.cs` (426 lines) | The manual-chase and due-work fixture, including how `CaseChaseSchedule` dates are arranged, for the `chases/manual` command's success case |
| `docs/desktop/03-gateway-api-and-data/endpoint-map.md` § Cases (Tasks rows) | The route strings, auth right (`PerformCasework`), idempotency (`yes (key)`) and return values the acceptance criteria are checked against — **and** the one stale cell (notes concurrency token), so you recognise it rather than implementing it |
| `docs/desktop/03-gateway-api-and-data/README.md` § 3 | The board-wide conventions this ticket inherits rather than invents: `operationKey` as a body field not a header, `expectedVersion`/`editLeaseToken` as explicit body fields, the `urn:pegasus:problem:<slug>` catalogue including `version-conflict`, `lease-conflict`, `lease-expired`, `operation-conflict` |
| `docs/desktop/01-inventory-and-parity/parity-matrix.md:56` | Row `PAR-11`, the parity claim this ticket's API half satisfies, and the fixtures it names |

## Ripple effects

- **`openapi/pegasus-v1.json`** gains eight paths and thirteen schemas. The snapshot test in
  `tests/Pegasus.Api.ContractTests` (created by [[GWY-004]]) fails until the snapshot is
  regenerated and committed. This is the mechanism working, not a break.
- **The generated Kiota client** under `src/Pegasus.Desktop.Infrastructure/Api/Generated/`
  (created by [[FND-031]]; generator and CI no-op check by [[GWY-005]]) regenerates from that
  snapshot. CI runs `git diff --exit-code` after regeneration, so an uncommitted regeneration is a
  red build.
- **[[FEAT-006]] is the caller** — this ticket is listed in its blockers. The desktop task strip
  cannot be built until these routes and the `CaseTaskResponseDto` shape land, and the response
  must carry both `version` and `caseVersion` or that slice needs a re-read after every command.
- **[[GWY-017]] (plan handle `DSK-03-17`)** adds compression, ETags and cancellation across every
  `/api/v1` endpoint including these eight; **[[GWY-018]] (plan handle `DSK-03-18`)** runs the
  independent authorization/contract gap review over them. Both are listed as blocked by this
  ticket. Leaving a command without its seven-case matrix shows up there, not here.
- **`GET /cases/{id}/history`** from [[GWY-007]] (plan handle `DSK-03-07`) is the surface the
  DOCS-012 facts assert through. If that section is not yet merged, this ticket's tests cannot be
  written as the body requires — check before starting.
- **Razor tests**: `CaseTasksWebTests.cs` and every custody/document test must stay green. The
  persistence change is behind the page, so a regression there means an event was duplicated
  rather than moved.
- **Nothing operator-facing reads `CaseHistory`**, so removing six rows from it changes no screen.
  Confirm with `grep -rn "CaseHistory" src/Pegasus.Web/ tests/` before the move (assumption
  A-GWY-3) rather than trusting the sentence.
- **No database migration, no grant change, no census edit** — verified in the research document
  against `20260729199000_RuntimeRoleReconciliation.cs` and
  `AzureSqlRuntimeRoleMigrationTests.cs`. If you find yourself writing a `Grant*` migration, you
  have introduced a new table that this ticket does not need.
- **`docs/` changes: none** beyond the regenerated snapshot. Named in the body.

## Out of scope

Recorded here because the ticket's Guardrails already forbid them and the reviewer should see the
decision, not infer it.

- `src/Pegasus.Core/Tasks/**` and `src/Pegasus.Web/Pages/Cases/Tasks.cshtml.cs` — the endpoint is a
  projection; changing either would make the API and the page two policy engines over one concept.
- Any `OperatorLabels` string. The five labels at `:389-394` are correct and become reachable
  unchanged. Rewording them is a separate design-authority change.
- The Evidence-tab markup and the design authority rows upstream DOCS-012 rewrites
  (`docs/design/README.md:617`, `:731`, and the Lucide registry) — [[FEAT-016]] (plan handle
  `DSK-05-16`) carries the Evidence-tab half and [[DUI-003]] (plan handle `DSK-06-03`) the glyph
  half. This ticket owns the notes rule only.
- Anything in `src/Pegasus.Infrastructure` other than the three named files and their named line
  ranges — including a shared `CaseWorkflowEventWriter` helper, which would be the natural
  refactor and is explicitly outside the named exception. Six inline writes is the intended shape.
- `EfExternalWorkStore.cs:210` — a `CaseHistory.Add` for a different event. The guardrail names
  four ranges in that file and this is not one of them.
- Activating the Case Details notes capability. Upstream CASE-004 keeps that surface
  non-persistent until its authority is accepted; `POST /cases/{id}/notes` is the existing
  Tasks-page command at parity. No note screen, no note taxonomy, no second write path.
- `Uploads/Request` and every other page that stays web-only
  (`endpoint-map.md` § Stays web-only).
- Azure: no write of any kind.
