# Open questions — FND-008

- [ ] For each of the 18 evidenced `DSK` durable outcomes, what exact `Horizon` (`Now`, `Next`, `Later`, or `Not planned`) and `Target release` (exact Semantic Version or `unallocated`) should be recorded? The repository does not state this mapping, and `Not planned`/`unallocated` are defined for permanent boundaries, so no placeholder assignment is truthful.

## Parked (explicitly deferred)

None.
