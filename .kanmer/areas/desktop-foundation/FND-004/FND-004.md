---
id: FND-004
type: ticket
title: >-
  DSK-00-04 · Verify the 208 seeded conversion tickets, keep the
  plan-handle-to-board-id join accurate, and verify the upstream carry-over
  batch
status: preparing
area: desktop-foundation
assignee: ''
profile: chore
stageEntered:
  preparing: '2026-08-24T21:21:39.500Z'
labels:
  - desktop-conversion
  - plan-00
  - phase-0
  - tier-1
groups:
  - EPIC-001
  - HZN-001
links: []
blocks:
  - FND-011
  - TOOL-007
docs_todo: true
archived: false
created: '2026-08-24T07:46:46.427Z'
updated: '2026-08-25T00:12:14.179Z'
---

## What

Verify the 208 conversion tickets that an agent session created on **2026-08-24** from the § 5 tables of the thirteen plan areas — one ticket per plan row, each with its area, profile, one `EPIC` and one `HZN` group, labels, `docs_todo`/`refs` and a `## Routing` block — then keep the plan-handle→board-id join accurate and count the upstream carry-over batch [[DSK-01-09]] owns against the corrected classification on Kanmer group `EPIC-014`, without creating anything.

## Why

Locked decision L-05 makes this board the executable form of the plan set: if a plan row has no ticket, the work silently disappears, and if a ticket exists twice the board double-counts an area's remaining work. The seeding session on 2026-08-24 created 208 tickets — the exact number of `| DSK-` rows across the thirteen area READMEs (00:13, 01:12, 02:16, 03:18, 04:15, 05:26, 06:16, 07:19, 08:19, 09:16, 10:18, 11:9, 12:11) — with `DSK-09-07` and `DSK-09-09` withdrawn and therefore absent. What remains is verification, the join that lets a human read `DSK-05-14` in a plan and find `FEAT-0xx` on the board (proposal §25; the plan handle lives in the ticket title for exactly this reason), and a count check over the carry-over batch. The plan row for this ticket assigns only "Create every DSK ticket from plans 01–12" — the carry-over batch is the plan-01 row's job and belongs to [[DSK-01-09]] alone. **Do not re-seed and do not re-create the carry-over**: `create_item` is ungated and a second pass duplicates every handle rather than failing. [[DSK-00-03]] verifies the board's groups and areas; this ticket verifies its tickets.

## Source of truth

- Plan row: `docs/desktop/00-governance-and-workflow/README.md` § 5 — `DSK-00-04`
- Plan detail: `docs/desktop/00-governance-and-workflow/README.md` § 3 — "Kanmer board shape for the fork" and "Ticket template (proposal §25 → Kanmer documents)"; `docs/desktop/README.md` — "Ticket IDs in these plans are planning handles … the plan handle goes into the ticket's `refs`/body so the two stay joined"
- Proposal: `docs/desktop/Pegasus_Native_Desktop_Design_Proposal.md` § 25 Ticket structure
- Repository evidence:
  - `docs/desktop/*/README.md` § 5 — the 208 ticket rows themselves (`grep -c '^| DSK-' docs/desktop/*/README.md`)
  - `docs/desktop/01-inventory-and-parity/upstream-kanmer-carryover.md:58-80` — the disposition categories and the recreation rule (`DSK-01-09` executes it; this ticket only counts the result)
  - Kanmer group `EPIC-014` (`get_group EPIC-014`) — the **corrected** carry-over classification from the 2026-08-24 coverage pass against upstream head `a5b28111`, which supersedes the 2026-08-23 triage table's own dispositions and totals and is what step 8 counts against
  - Kanmer group document `HZN-001/board-conventions.md` § *Upstream ids versus board ids* (`get_group_doc HZN-001 board-conventions.md`) — the authoritative 19-row upstream↔board join and the rule that a bare `<PREFIX>-<nnn>` is a fork board id while an upstream id is always written `upstream <ID>`
  - `AGENTS.md:1-22` — Kanmer operating block (gates, one gated boundary per move, and the rule that items are referenced by wiki-links of the form `DSK-xx-yy`)
- Binding decisions: **L-05** the board is seeded from these plans; **L-04** every ticket names its subagent, skills and MCP tools — the `## Routing` block is the check for it.
- Depends on: `DSK-00-03` — the areas and groups a ticket references must exist first. `DSK-01-09` — it owns the carry-over batch that step 8 counts (the § 5 row cites the area 00 handle only; the carry-over triage and its execution live in area 01).

## Routing

- **Subagent**: — (parent session; `pegasus-parity-researcher` — `.codex/agents/pegasus-parity-researcher.toml` — for the read-only cross-check of plan rows against board tickets)
- **Skills**, loaded in this order: `pegasus-desktop` (`.agents/skills/project/pegasus-desktop/SKILL.md`) → `kanmer-tickets` (`.grok/skills/kanmer-tickets/SKILL.md`) → `kanmer-groom` (`.grok/skills/kanmer-groom/SKILL.md`)
- **MCP**: Kanmer (`get_status`, `list_board`, `list_items`, `search_items`, `get_item`, `get_group`, `get_group_doc`, `create_items`, `update_item`, `link_doc`, `set_ticket_doc`, `get_doc_gates`, `take_ticket`, `append_scratch`, `move_item`)
- **Kanmer pipeline** for profile `chore`: `kanmer-tickets` → `kanmer-plan` → `kanmer-execute` → `kanmer-review` → `kanmer-verify` → `kanmer-closeout` (call `get_doc_gates <id>` before every move; a move crosses at most one gated boundary)
- **Reviewer**: `pegasus-desktop-reviewer` — an agent that did not implement (`AGENTS.md` § Repository task workflow step 5)

## Implementation steps

1. Orient: read the plan row and § 3 "Ticket template", then `docs/desktop/01-inventory-and-parity/upstream-kanmer-carryover.md`. Call `get_doc_gates <this ticket id>`, then `take_ticket`. **Never create a handle that already exists** — `search_items` for it first.
2. Count the source of truth: `grep -c '^| DSK-' docs/desktop/*/README.md` — expected 13, 12, 16, 18, 15, 26, 16, 19, 19, 16, 18, 9, 11 for plan areas 00…12, total **208**. `DSK-09-07` and `DSK-09-09` are withdrawn: **no ticket carries either handle**. Both strings do still appear in prose — in this step, and in [[DSK-09-08]] step 1, which records that their spikes were withdrawn — and those two references are deliberate and correct. The check is that no ticket exists under either handle, not that the string appears nowhere.
3. Count the board: `list_items` with `label: "desktop-conversion"` — expected 208. Then per plan area, `list_items` with `label: "plan-00"` … `plan-12` and compare each count with step 2.
4. Build the join: for each plan area run `search_items` with the handle prefix (`DSK-05-`) and record `handle → board id → area → profile → groups`. Write the table into this ticket with `set_ticket_doc` (a `reference/` document is ungated). **Do not** write it into the repository tree: a new `.md` outside `docs/(prd|frd|adr|design|desktop)` fails the CI `documentation` job (`scripts/Test-MarkdownPlacement.ps1:31`).
5. Spot-check ten tickets spread across areas with `get_item`. Each must have: the plan handle at the front of the title; the board area from § 2 of the seeding plan; the profile from its plan row; exactly one `EPIC-0xx` and one `HZN-0xx` group; labels `desktop-conversion`, `plan-<NN>`, `phase-<N>` and its tier label; `docs_todo: true` where the governing document is a conversion ADR/FRD that does not exist yet; and a body carrying `## Routing` plus numbered `## Implementation steps`.
6. Probe one gate: `get_doc_gates` on any `feature` ticket — expected `leave-backlog: [governing-doc]`, satisfied by `docs_todo: true`. Record the output.
7. Fill gaps only: for any plan row with no ticket, create it with `create_items` in **batches of at most 6**, using the body template in § 3 of the area 00 plan and the row's own acceptance, verification, tier and routing columns. Check each entry's `{ok, item|error}` result and retry failures — a `refs` path that does not exist fails the whole entry. This is the only creation this ticket performs, and it covers conversion plan rows only.
8. Carry-over count, after [[DSK-01-09]] is done — **verification only; create nothing here**: read `docs/desktop/01-inventory-and-parity/upstream-kanmer-carryover.md` **together with** `get_group EPIC-014`, whose corrected classification supersedes the 2026-08-23 triage table's dispositions and totals. That 2026-08-24 coverage pass reconciled the **114** open, non-archived upstream tickets at head `a5b28111` as **19 imported / 21 amended / 75 dropped**: 19 imports + 20 amend-only upstream ids + 75 drops = 114, because `DOCS-001`, `TICK-208`, `ENG-014`, `ENG-015` and `CASE-022` are each both imported and named in an amendment and are counted once, in the import list. Confirm on the board with `list_items` — `group: EPIC-014`, then one call per fork area — that it holds exactly **19** `upstream:` tickets, each carrying `EPIC-014` as its only group with **no** conversion `EPIC` and **no** `HZN`, split `automation-integrations` 2, `engineering-assessment` 2, `platform-operations` 2, `intake-processing` 7, `documents-reports` 3, `case-reference-workflow` 2 and `desktop-ui` 1, with `delivery-repository`, `mail-communications` and `pr-review` holding **zero**. Confirm no upstream id appears twice, that the 233 `done` and 114 archived upstream tickets were not recreated, and that no amend-list or drop-list row was imported. `unchanged-backlog` is **not** a blanket exclusion — it is safe only for rows that have a `docs/capabilities.md` row, and there is none for `INTK-026`, `INTK-031` or `INTK-032`, so all three are **expected** imports rather than defects. The withdrawn figures are wrong and must never be restated: there are not 18 `desktop-screen-spec` tickets in `desktop-ui`, not 39 across the domain areas, not 57 from 56 ids, and `upstream CASE-009` is **not** recreated twice — it is an amendment to [[DSK-07-11]] and [[DSK-05-03]], not two tickets. Re-derive every number from `list_items` before you write it down; do not copy a figure from any document, this ticket included. Any mismatch is reported as a finding on [[DSK-01-09]] — do **not** create, relabel or retitle a carry-over ticket here: `create_items` is ungated, so a second creating pass duplicates the batch instead of failing.
9. Reconcile the plan-00 board-shape table with what was actually seeded — **the whole table, not one or two rows**. The table in `docs/desktop/00-governance-and-workflow/README.md` § "Kanmer board shape for the fork" (`:228-248`) says `desktop-foundation` holds "area plans 02 (and 00 governance tickets)", `gateway-api` "area plan 03, 04 (gateway side)", `desktop-features` "area plan 05 slices, 07 desktop side", and gives plan folders 10 and 11 no area at all. The seeded board says otherwise; re-establish each line with `list_items` before writing anything down:
   - `desktop-foundation` holds plans **00, 01, 02 and the desktop half of 04** (`plan-00` 13, `plan-01` 12, `plan-02` 16, `plan-04` 9 — FND-042…FND-050);
   - `gateway-api` holds **plan 03 and the gateway half of 04** (`plan-03` 18, `plan-04` 6 — GWY-019…GWY-024);
   - `desktop-features` holds **plans 05 and 07 in full** (`plan-05` 26, `plan-07` 19 — FEAT-027…FEAT-045), of which ten are gateway- or build-side work: FEAT-027, FEAT-028, FEAT-029, FEAT-031, FEAT-034, FEAT-035, FEAT-037, FEAT-039, FEAT-042, FEAT-045;
   - `platform-operations` holds **plans 10 and 11** (`plan-10` 18, `plan-11` 9), a deliberate deviation already recorded in the Kanmer group document `HZN-001/board-conventions.md` § 2 "Deviation to note" (`get_group_doc HZN-001 board-conventions.md`) — cite that record, do not re-decide it.
   Two outcomes are honest, and the choice is the operator's to make and this ticket's to record — never to guess:
   **(a) amend the plan-00 table to match the seed** — `desktop-foundation` = "area plans 00, 01, 02 and the desktop half of 04", `gateway-api` = "area plan 03, 04 (gateway side)", `desktop-features` = "area plan 05 slices, area plan 07"; or
   **(b) move the ten gateway-side plan-07 tickets** to `gateway-api` with `update_item(area: "gateway-api")` on each (their `## Routing` blocks name `pegasus-gateway-dev` or `pegasus-release-packager` and their scope boundaries touch `src/Pegasus.Web`, `src/Pegasus.Contracts` and `eng/build/`, not `src/Pegasus.Desktop`), and amend the `gateway-api` row to "area plan 03, 04 (gateway side), 07 (gateway side)", leaving the `desktop-features` row as it reads.
   Under **both** outcomes the `desktop-foundation` row is wrong as written and must be corrected. Record the chosen outcome, who chose it, its reason and its date in the ticket plan document and apply exactly that outcome here; an unrecorded or assumed choice is a defect. Nothing is unworkable while it is open — `get_doc_gates` gives `desktop-features` and `gateway-api` identical gate rules — but an area-scoped sweep over `gateway-api` silently misses ten tickets until it is settled.
10. Re-run steps 3 and 4 after any creation or area change so the counts and the join stay true, and append the new totals to the ticket scratch with `append_scratch`.
11. Leave every ticket in `backlog`: seeding sets no status and this ticket calls no `move_item` on another ticket.
12. Write the ticket proof as a `command-log` containing the final counts, the ten spot-checks, the carry-over count from step 8, the recorded board-shape outcome from step 9 and the `get_doc_gates` probe.

## Acceptance criteria

- [ ] `list_items` with `label: "desktop-conversion"` returns 208 conversion tickets, and the per-area counts match `grep -c '^| DSK-' docs/desktop/*/README.md`.
- [ ] No plan row is missing a ticket and no handle exists twice; **no ticket exists under the withdrawn handles `DSK-09-07` and `DSK-09-09`** — prose references to the withdrawal are expected and are not defects.
- [ ] The plan-handle→board-id join exists as a ticket document and is regenerated after every creation.
- [ ] Ten spot-checked tickets carry the handle, area, profile, one EPIC, one HZN, the labels, the `docs_todo` flag and a `## Routing` block.
- [ ] The carry-over batch is counted against `EPIC-014`'s corrected classification and matches it — 19 imported / 21 amended / 75 dropped, reconciling as 19 + 20 amend-only ids + 75 = the 114 open upstream tickets at head `a5b28111`; the board holds 19 `upstream:` tickets, each in `EPIC-014` alone with no conversion `EPIC`/`HZN`, split 2 / 2 / 2 / 7 / 3 / 2 / 1 across `automation-integrations`, `engineering-assessment`, `platform-operations`, `intake-processing`, `documents-reports`, `case-reference-workflow` and `desktop-ui`, with `delivery-repository`, `mail-communications` and `pr-review` empty; `INTK-026`, `INTK-031` and `INTK-032` are present as expected imports because no `docs/capabilities.md` row holds them; no upstream id appears twice; and this ticket created none of them.
- [ ] Every figure in the step 8 record was re-derived from `list_items` at run time rather than copied from the triage table or from this ticket, and the withdrawn 57 / 56 / 18 / 39 figures appear nowhere in the proof.
- [ ] The plan-00 board-shape table and the seeded board agree across the whole table: the `desktop-foundation` row is corrected, plans 10 and 11 are reconciled against `HZN-001/board-conventions.md` § 2, and either the `desktop-features` row is amended to plan 07 in full or the ten gateway-side plan-07 tickets (FEAT-027, -028, -029, -031, -034, -035, -037, -039, -042, -045) are moved to `gateway-api` with that row amended — with the chosen outcome, its reason and its date recorded.

## Verification

- [ ] `list_items` with `label: "desktop-conversion"` — expected: 208 summaries, all `backlog`.
- [ ] `grep -c '^| DSK-' docs/desktop/*/README.md` — expected: 13 12 16 18 15 26 16 19 19 16 18 9 11.
- [ ] `get_doc_gates <a feature ticket id>` — expected: `leave-backlog` names `governing-doc` and is satisfied by `docs_todo`.
- [ ] `search_items` for `DSK-09-07`, then the same for `DSK-09-09` — expected: **no ticket whose title begins with either handle**. Read the `title` of every hit rather than the hit count. `search_items` is full-text over id, title, body, labels and assignee, so both searches legitimately return results today — this ticket (step 2) and [[DSK-09-08]] (step 1), each of which names the withdrawn handle in prose. A non-empty result set is not a failure; a hit whose title begins `DSK-09-07 ·` or `DSK-09-09 ·` is.
- [ ] `list_items` with `group: EPIC-014` — expected: exactly 19 `upstream:` tickets, each carrying `EPIC-014` as its only group, none created by this ticket.
- [ ] `list_items` per fork carry-over area — expected: `automation-integrations` 2, `engineering-assessment` 2, `platform-operations` 2, `intake-processing` 7, `documents-reports` 3, `case-reference-workflow` 2, `desktop-ui` 1; and `delivery-repository`, `mail-communications` and `pr-review` zero.
- [ ] `search_items` for `upstream:CASE-009` — expected: **no ticket whose title begins `upstream:CASE-009 ·`**. Same shape as the withdrawn-handle check above: the search returns this ticket, because step 8 names the id in prose. Upstream `CASE-009` is an amendment to [[DSK-07-11]] and [[DSK-05-03]], never a recreated ticket.
- [ ] `sed -n '228,248p' docs/desktop/00-governance-and-workflow/README.md` compared with `list_items` per `plan-NN` label — expected: every row of the board-shape table describes the areas the board actually holds, under the outcome recorded in step 9.

## Evidence tier

Tier 1 — Static/build/architecture. Board state and plan-row counts only; no application behaviour is claimed.

## Documentation changes

- `docs/desktop/00-governance-and-workflow/README.md` § "Kanmer board shape for the fork" (the area table at `:228-248`) — the rows corrected to the outcome step 9 records: the `desktop-foundation` row under either outcome, plus either the `desktop-features` row (outcome a) or the `gateway-api` row (outcome b). Plans 10 and 11 keep the deviation already recorded in the Kanmer group document `HZN-001/board-conventions.md` § 2 — cite it rather than restating the decision.
- The carry-over document's `refs: upstream:<ID>` instruction, its stale 109-row totals and its withdrawn 57 / 56 / 18 / 39 figures are corrected by [[DSK-01-09]], the ticket that owns those tickets — not here.

## Guardrails

- **Azure**: no write.
- **Scope boundary**: Kanmer tickets and ticket documents, plus the one plan-document correction step 9 owns (`docs/desktop/00-governance-and-workflow/README.md` § "Kanmer board shape for the fork") and no other repository file. The only creation is step 7's gap-fill over conversion plan rows; the upstream carry-over batch belongs solely to [[DSK-01-09]] and is only counted here. No group or area creation (that is [[DSK-00-03]]), no `move_item`, no `take_ticket` on another agent's ticket.
- **Traps**: `create_item`/`create_items` are ungated — a second seeding or carry-over run duplicates rather than fails; `refs` paths must exist or the whole entry fails; capability IDs (`CASE-17`) are not ticket IDs (`CASE-017`), which is why the plans use `DSK-<area>-<nn>` handles; an unticked `- [ ]` in `open-questions/` blocks a move. **`search_items` is full-text over id, title, body, labels and assignee**, so "expected: no results" is never a safe way to assert that a ticket does not exist — a body that merely mentions the handle matches. Assert on the `title` of each hit instead; every absence check in this ticket is written that way. **The 2026-08-23 triage table's own figures are withdrawn** — 109 rows against 114 open upstream tickets, and its 57 / 56 / 18 / 39 / 53 counts are all wrong; `EPIC-014` carries the corrected 19 / 21 / 75 classification and every number in step 8 is re-derived from `list_items`, never copied. **`unchanged-backlog` is only safe where a `docs/capabilities.md` row exists**, so `INTK-026`, `INTK-031` and `INTK-032` are expected imports and reporting them as defects would report the correct board as wrong. **Upstream ids are not fork board ids** — a bare `<PREFIX>-<nnn>` on this board is a fork board id and an upstream id is written `upstream <ID>`; `HZN-001/board-conventions.md` § *Upstream ids versus board ids* holds the authoritative 19-row join and [[DSK-01-09]] step 3 mirrors it. Step 9's option (b) changes a ticket's board area with `update_item`, which moves its folder and keeps its id — it is not `move_item` and must not be attempted with one.
- **Concern**: this row carries three distinct jobs (verify the seeded tickets, maintain the join, count the carry-over batch) plus the board-shape reconciliation of step 9. The plan is followed rather than split, per the seeding contract; the carry-over creation that this ticket once described belongs solely to [[DSK-01-09]], so a shortfall found in step 8 is raised as a finding on that ticket and never fixed by creating tickets here.
- **Simplification pass** (`AGENTS.md` step 4): required over this branch diff before the PR, recorded under a dated `## Simplification pass` heading in the plan document — `n/a — docs-only`.

## Outcome

_Filled at closeout._
