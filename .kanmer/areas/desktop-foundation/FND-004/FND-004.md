---
id: FND-004
type: ticket
title: >-
  DSK-00-04 · Verify the 208 seeded conversion tickets, keep the
  plan-handle-to-board-id join accurate, and verify the upstream carry-over
  batch
status: backlog
area: desktop-foundation
assignee: ''
profile: chore
labels:
  - desktop-conversion
  - plan-00
  - phase-0
  - tier-1
groups:
  - EPIC-001
  - HZN-001
links: []
blocks:
  - FND-011
  - TOOL-007
docs_todo: true
archived: false
created: '2026-08-24T07:46:46.427Z'
updated: '2026-08-24T09:13:31.484Z'
---

## What

Verify the 208 conversion tickets that an agent session created on **2026-08-24** from the § 5 tables of the thirteen plan areas — one ticket per plan row, each with its area, profile, one `EPIC` and one `HZN` group, labels, `docs_todo`/`refs` and a `## Routing` block — then keep the plan-handle→board-id join accurate and, once [[DSK-01-09]] has recreated the carried-over tickets, count that batch against the triage table without creating anything.

## Why

Locked decision L-05 makes this board the executable form of the plan set: if a plan row has no ticket, the work silently disappears, and if a ticket exists twice the board double-counts an area's remaining work. The seeding session on 2026-08-24 created 208 tickets — the exact number of `| DSK-` rows across the thirteen area READMEs (00:13, 01:12, 02:16, 03:18, 04:15, 05:26, 06:16, 07:19, 08:19, 09:16, 10:18, 11:9, 12:11) — with `DSK-09-07` and `DSK-09-09` withdrawn and therefore absent. What remains is verification, the join that lets a human read `DSK-05-14` in a plan and find `FEAT-0xx` on the board (proposal §25; the plan handle lives in the ticket title for exactly this reason), and a count check over the carry-over batch. The plan row for this ticket assigns only "Create every DSK ticket from plans 01–12" — the carry-over batch is the plan-01 row's job and is created by [[DSK-01-09]] alone. **Do not re-seed and do not re-create the carry-over**: `create_item` is ungated and a second pass duplicates every handle rather than failing. [[DSK-00-03]] verifies the board's groups and areas; this ticket verifies its tickets.

## Source of truth

- Plan row: `docs/desktop/00-governance-and-workflow/README.md` § 5 — `DSK-00-04`
- Plan detail: `docs/desktop/00-governance-and-workflow/README.md` § 3 — "Kanmer board shape for the fork" and "Ticket template (proposal §25 → Kanmer documents)"; `docs/desktop/README.md` — "Ticket IDs in these plans are planning handles … the plan handle goes into the ticket's `refs`/body so the two stay joined"
- Proposal: `docs/desktop/Pegasus_Native_Desktop_Design_Proposal.md` § 25 Ticket structure
- Repository evidence:
  - `docs/desktop/*/README.md` § 5 — the 208 ticket rows themselves (`grep -c '^| DSK-' docs/desktop/*/README.md`)
  - `docs/desktop/01-inventory-and-parity/upstream-kanmer-carryover.md:58-80` — the disposition categories and the recreation rule (`DSK-01-09` executes it; this ticket only counts the result)
  - `AGENTS.md:1-22` — Kanmer operating block (gates, one gated boundary per move, `[[ID]]` wiki-links)
- Binding decisions: **L-05** the board is seeded from these plans; **L-04** every ticket names its subagent, skills and MCP tools — the `## Routing` block is the check for it.
- Depends on: `DSK-00-03` — the areas and groups a ticket references must exist first. `DSK-01-09` — it creates the carry-over batch that step 8 counts (the § 5 row cites the area 00 handle only; the carry-over triage and its recreation live in area 01).

## Routing

- **Subagent**: — (parent session; `pegasus-parity-researcher` — `.codex/agents/pegasus-parity-researcher.toml` — for the read-only cross-check of plan rows against board tickets)
- **Skills**, loaded in this order: `pegasus-desktop` (`.agents/skills/project/pegasus-desktop/SKILL.md`) → `kanmer-tickets` (`.grok/skills/kanmer-tickets/SKILL.md`) → `kanmer-groom` (`.grok/skills/kanmer-groom/SKILL.md`)
- **MCP**: Kanmer (`get_status`, `list_board`, `list_items`, `search_items`, `get_item`, `create_items`, `update_item`, `link_doc`, `set_ticket_doc`, `get_doc_gates`, `take_ticket`, `append_scratch`, `move_item`)
- **Kanmer pipeline** for profile `chore`: `kanmer-tickets` → `kanmer-plan` → `kanmer-execute` → `kanmer-review` → `kanmer-verify` → `kanmer-closeout` (call `get_doc_gates <id>` before every move; a move crosses at most one gated boundary)
- **Reviewer**: `pegasus-desktop-reviewer` — an agent that did not implement (`AGENTS.md` § Repository task workflow step 5)

## Implementation steps

1. Orient: read the plan row and § 3 "Ticket template", then `docs/desktop/01-inventory-and-parity/upstream-kanmer-carryover.md`. Call `get_doc_gates <this ticket id>`, then `take_ticket`. **Never create a handle that already exists** — `search_items` for it first.
2. Count the source of truth: `grep -c '^| DSK-' docs/desktop/*/README.md` — expected 13, 12, 16, 18, 15, 26, 16, 19, 19, 16, 18, 9, 11 for plan areas 00…12, total **208**. `DSK-09-07` and `DSK-09-09` are withdrawn and must not appear anywhere.
3. Count the board: `list_items` with `label: "desktop-conversion"` — expected 208. Then per plan area, `list_items` with `label: "plan-00"` … `plan-12` and compare each count with step 2.
4. Build the join: for each plan area run `search_items` with the handle prefix (`DSK-05-`) and record `handle → board id → area → profile → groups`. Write the table into this ticket with `set_ticket_doc` (a `reference/` document is ungated). **Do not** write it into the repository tree: a new `.md` outside `docs/(prd|frd|adr|design|desktop)` fails the CI `documentation` job (`scripts/Test-MarkdownPlacement.ps1:31`).
5. Spot-check ten tickets spread across areas with `get_item`. Each must have: the plan handle at the front of the title; the board area from § 2 of the seeding plan; the profile from its plan row; exactly one `EPIC-0xx` and one `HZN-0xx` group; labels `desktop-conversion`, `plan-<NN>`, `phase-<N>` and its tier label; `docs_todo: true` where the governing document is a conversion ADR/FRD that does not exist yet; and a body carrying `## Routing` plus numbered `## Implementation steps`.
6. Probe one gate: `get_doc_gates` on any `feature` ticket — expected `leave-backlog: [governing-doc]`, satisfied by `docs_todo: true`. Record the output.
7. Fill gaps only: for any plan row with no ticket, create it with `create_items` in **batches of at most 6**, using the body template in § 3 of the area 00 plan and the row's own acceptance, verification, tier and routing columns. Check each entry's `{ok, item|error}` result and retry failures — a `refs` path that does not exist fails the whole entry. This is the only creation this ticket performs, and it covers conversion plan rows only.
8. Carry-over count, after [[DSK-01-09]] is done — **verification only; create nothing here**: read its triage table in `docs/desktop/01-inventory-and-parity/upstream-kanmer-carryover.md`, then `list_items` per fork area (`desktop-ui`, `automation-integrations`, `case-reference-workflow`, `delivery-repository`, `documents-reports`, `intake-processing`, `mail-communications`, `platform-operations`, `pr-review`) and confirm the counts match the table's dispositions: 18 `desktop-screen-spec` tickets in `desktop-ui` and 39 `gateway-worker-ticket`/`report-decision` tickets across the domain areas, 57 in all from 56 distinct upstream ids (`CASE-009` is recreated twice). Confirm no `unchanged-backlog` row was recreated, that the 233 done and 114 archived upstream tickets were not recreated, and that no upstream id appears twice. Any mismatch is reported as a finding on [[DSK-01-09]] — do **not** create, relabel or retitle a carry-over ticket here: `create_items` is ungated, so a second creating pass duplicates the batch instead of failing.
9. Re-run steps 3 and 4 after any creation so the counts and the join stay true, and append the new totals to the ticket scratch with `append_scratch`.
10. Leave every ticket in `backlog`: seeding sets no status and this ticket calls no `move_item` on another ticket.
11. Write the ticket proof as a `command-log` containing the final counts, the ten spot-checks, the carry-over count from step 8 and the `get_doc_gates` probe.

## Acceptance criteria

- [ ] `list_items` with `label: "desktop-conversion"` returns 208 conversion tickets, and the per-area counts match `grep -c '^| DSK-' docs/desktop/*/README.md`.
- [ ] No plan row is missing a ticket and no handle exists twice; `DSK-09-07` and `DSK-09-09` are absent.
- [ ] The plan-handle→board-id join exists as a ticket document and is regenerated after every creation.
- [ ] Ten spot-checked tickets carry the handle, area, profile, one EPIC, one HZN, the labels, the `docs_todo` flag and a `## Routing` block.
- [ ] The carry-over batch created by [[DSK-01-09]] is counted against the triage table and matches it — 57 tickets from 56 upstream ids, no `unchanged-backlog` row recreated, no upstream id twice — and this ticket created none of them.

## Verification

- [ ] `list_items` with `label: "desktop-conversion"` — expected: 208 summaries, all `backlog`.
- [ ] `grep -c '^| DSK-' docs/desktop/*/README.md` — expected: 13 12 16 18 15 26 16 19 19 16 18 9 11.
- [ ] `get_doc_gates <a feature ticket id>` — expected: `leave-backlog` names `governing-doc` and is satisfied by `docs_todo`.
- [ ] `search_items` for `DSK-09-07` — expected: no results.
- [ ] `list_items` for `desktop-ui` and the eight domain carry-over areas — expected: 57 `upstream:` tickets in total, matching the triage table, none created by this ticket.

## Evidence tier

Tier 1 — Static/build/architecture. Board state and plan-row counts only; no application behaviour is claimed.

## Documentation changes

- `None.` The carry-over document's `refs: upstream:<ID>` instruction is corrected by [[DSK-01-09]], the ticket that creates those tickets.

## Guardrails

- **Azure**: no write.
- **Scope boundary**: Kanmer tickets and ticket documents only, and no repository file edit at all. The only creation is step 7's gap-fill over conversion plan rows; the upstream carry-over batch is created solely by [[DSK-01-09]] and is only counted here. No group or area creation (that is [[DSK-00-03]]), no `move_item`, no `take_ticket` on another agent's ticket.
- **Traps**: `create_item`/`create_items` are ungated — a second seeding or carry-over run duplicates rather than fails; `refs` paths must exist or the whole entry fails; capability IDs (`CASE-17`) are not ticket IDs (`CASE-017`), which is why the plans use `DSK-<area>-<nn>` handles; an unticked `- [ ]` in `open-questions/` blocks a move.
- **Concern**: this row carries three distinct jobs (verify the seeded tickets, maintain the join, count the carry-over batch). The plan is followed rather than split, per the seeding contract; the carry-over creation that this ticket once described belongs solely to [[DSK-01-09]], so a shortfall found in step 8 is raised as a finding on that ticket and never fixed by creating tickets here.
- **Simplification pass** (`AGENTS.md` step 4): required over this branch diff before the PR, recorded under a dated `## Simplification pass` heading in the plan document — `n/a — docs-only`.

## Outcome

_Filled at closeout._
