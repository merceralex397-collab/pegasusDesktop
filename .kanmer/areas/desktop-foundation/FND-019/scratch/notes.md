2026-08-25 orientation and verification command checkpoint in `.worktrees/fnd-019`: exact commands confirmed `SignIn.cshtml.cs:64 CheckPasswordSignInAsync(... lockoutOnFailure: false)`; `Program.cs:275 AddRateLimiter`, FixedWindowRateLimiter at 301,312,320 and use at 802; `StaffSessionPolicy.cs:9-13` idle 2h, absolute 8h, 10/client/min, 100 global; `AutomationMcpExtensions.cs:41 AllowClientCredentialsFlow()` and `:47 AllowRefreshTokenFlow()`. No AllowPasswordFlow match in the current source.

Research checkpoint 2026-08-25: read-only evidence closed the main current-state questions. `Test-MigrationGrants.ps1` passed: 64 migration files checked, every created table granted or exempted. OpenIddict uses existing OpenIddictApplications, OpenIddictAuthorizations, OpenIddictScopes, and OpenIddictTokens; current Web grants are SELECT/INSERT/UPDATE for applications/authorizations/tokens and SELECT for scopes, with delete denied by AutomationActorOpenIddict. `StaffActorFactory.TryCreate` requires non-empty Guid subject, at least one exact-case defined StaffRole (Administrator, Engineer, User), and rejects unknown/empty roles. Staff cookie revalidates security stamp and IsEnabled on every request; token-side IsEnabled interval is not yet settled. `MustChangePassword` is currently enforced by authenticated-request middleware redirect to /Account/PasswordChange; no token endpoint contract exists. DevelopmentOffline uses a local-only authentication handler, DB-backed seeded local administrator, and no token equivalent. AutomationClientRegistry idempotently seeds/reconciles the OpenIddict client on token/authorize requests, not via migration data seed. Record 3: ApprovedInboxPollStateEntity has MailboxId, address, Cursor, DueAtUtc, lease fields, LastCompletedAtUtc, LastFailureCode; MailPollHealth and GetRetainedMailFreshness already expose last completion/failure/freshness. Worker runtime grants include ApprovedInboxPollStates/ApprovedMailboxes/ApprovedInboxPoisonMessages and retained-mail read/write tables are covered by the Web/Worker matrix. Current code still carries ADR-0024 migration warning: stable mailbox identity is tracked work and current resume/adoption is superseded by a per-mailbox fresh start before Phase 5 Inbox. Azure MCP read-only functionapp_get succeeded for pegasus-prod-worker-252ow37gij in rg-pegasus-prod: Running, UK South, plan pegasus-prod-worker-plan-252ow37gij, release tag 0.1.0-alpha.1; the MCP exposes app details but no setting-name command, so no setting values were retrieved. Microsoft Learn citations fetched 2026-08-25: JWT access-token handling, OIDC offline_access, Credential Locker, and DPAPI. No Azure writes or product-code edits performed.
