2026-08-26 implementation checkpoint: created Pegasus.Server.slnf over Pegasus.slnx, added two architecture facts, and documented the Linux/Windows entry points in docs/runbook.md. Microsoft Learn mechanism research was refreshed before implementation. Restore and Release build passed for all 7 projects on Windows; focused architecture tests passed 2/2; full architecture suite passed 103/103; documentation links and markdown placement passed. Simplification pass recorded in plan. Linux execution was not performed. Next: obtain independent review, open PR to dev, then await CI/review before merge.

Independent review checkpoint: reviewer found one valid medium contradiction in docs/runbook.md, where the locked commands still implied Pegasus.slnx on Linux. Corrected the section with separate Windows/full-solution and Linux/server-filter command blocks; doc links, Markdown placement, and git diff --check passed. Fresh independent review required before PR.

Fresh independent review passed after correction: docs/runbook.md now has separate Windows Pegasus.slnx and Linux Pegasus.Server.slnf locked restore/build/test commands; contradiction resolved, no merge-blocking findings. Reviewer confirmed documentation links and Markdown placement pass.
